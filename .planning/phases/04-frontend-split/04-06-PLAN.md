---
phase: 04-frontend-split
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/components/StatusBar.jsx
  - frontend/components/TerminalLine.jsx
  - frontend/components/TerminalInput.jsx
  - frontend/components/TerminalPanel.jsx
  - frontend/components/FindingsPanel.jsx
  - frontend/components/panels-batch2.smoke.test.jsx
autonomous: true
requirements: [UI-02]
user_setup: []

must_haves:
  truths:
    - "StatusBar (with EngagementTimer), TerminalPanel (with TerminalLine + TerminalInput), and FindingsPanel each exist as standalone importable files under frontend/components/ (UI-02)"
    - "Each preserves its established panel shape, REST/WS behavior, and styling verbatim"
    - "The report file-download flow (FindingsPanel) and terminal REST-exec + inline-error flow are preserved unchanged"
  artifacts:
    - path: "frontend/components/StatusBar.jsx"
      provides: "Top status bar + co-located EngagementTimer, three-way connection dots"
    - path: "frontend/components/TerminalPanel.jsx"
      provides: "Terminal panel (auto-scroll) composing TerminalLine + TerminalInput"
    - path: "frontend/components/TerminalInput.jsx"
      provides: "REST POST /terminal/exec with inline error banner"
    - path: "frontend/components/FindingsPanel.jsx"
      provides: "Findings list + report download (POST /report/*, blob download)"
    - path: "frontend/components/panels-batch2.smoke.test.jsx"
      provides: "Import + shallow-render smoke tests"
  key_links:
    - from: "frontend/components/TerminalPanel.jsx"
      to: "frontend/components/TerminalLine.jsx, frontend/components/TerminalInput.jsx"
      via: "composition (co-located sub-parts per D-04)"
      pattern: "TerminalLine|TerminalInput"
---

<objective>
Extract the three heavier panel groups from `App.jsx` into standalone files (D-04): `StatusBar` (co-located with its `EngagementTimer` sub-component), `TerminalPanel` (co-located with `TerminalLine` and `TerminalInput` per D-04's explicit "+ its TerminalLine/TerminalInput sub-parts" note), and `FindingsPanel` (the only file-I/O flow in the codebase — report blob download). All are rated "exact" verbatim extractions in the PATTERNS map. As with 04-05, several REST targets (`/terminal/exec`, `/report/*`) do not exist in the current backend (RESEARCH.md Pitfall 5) — preserve the calls verbatim; their non-functional appearance is pre-existing and out of scope.

Purpose: Deliver the remaining standalone panels for UI-02; enables 04-07 to wrap each in an ErrorBoundary.
Output: Five new files (StatusBar, TerminalLine, TerminalInput, TerminalPanel, FindingsPanel) + a smoke test. Does NOT edit App.jsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-frontend-split/04-CONTEXT.md
@.planning/phases/04-frontend-split/04-PATTERNS.md
@.planning/phases/04-frontend-split/04-UI-SPEC.md
@frontend/lib/constants.js
@frontend/lib/format.js

<interfaces>
<!-- Exact App.jsx line ranges to extract verbatim (from 04-PATTERNS.md, App.jsx = 1505 lines). -->
StatusBar + EngagementTimer            — App.jsx lines 188-263 (co-locate both in StatusBar.jsx); reads chatConnected/eventsConnected/terminalConnected as props (D-08 keeps App.jsx the state owner). "connected" for CHAT now means session-acked (UI-SPEC) — wire the redefined boolean, visuals unchanged.
TerminalLine({ line })                 — App.jsx lines 483-536 (may use renderPayload from lib/format)
TerminalInput                          — App.jsx lines 538-608 (REST POST /terminal/exec + inline error state; auto-focus)
TerminalPanel                          — App.jsx lines 610-673 (auto-scroll bottomRef/scrollIntoView; composes TerminalLine + TerminalInput)
FindingsPanel                          — App.jsx lines 675-872 (triggerDownload/downloadReport blob flow 682-717; uses SEVERITY_MAP, REPORT_FORMATS_UI, REPORT_FRAMEWORKS from lib/constants)
Panel shape convention: <div className="panel flex flex-col h-full"><div className="panel-header">…</div>{content}</div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract StatusBar (+EngagementTimer) and the Terminal trio (TerminalLine, TerminalInput, TerminalPanel)</name>
  <files>frontend/components/StatusBar.jsx, frontend/components/TerminalLine.jsx, frontend/components/TerminalInput.jsx, frontend/components/TerminalPanel.jsx</files>
  <read_first>
    - frontend/src/App.jsx (StatusBar+EngagementTimer 188-263; TerminalLine 483-536; TerminalInput 538-608; TerminalPanel 610-673)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the StatusBar and TerminalPanel/TerminalLine/TerminalInput sections)
    - frontend/lib/format.js (renderPayload/fmtElapsed if referenced), frontend/lib/constants.js (EVENT_ICONS if referenced)
    - .planning/phases/04-frontend-split/04-UI-SPEC.md (StatusBar CHAT status = session-acked semantic; the "REST POST + inline error banner" terminal pattern; color/typography to preserve)
  </read_first>
  <action>
    Create `StatusBar.jsx` co-locating both `StatusBar` (default export) and `EngagementTimer` (App.jsx lines 188-263) verbatim — StatusBar keeps receiving `chatConnected`/`eventsConnected`/`terminalConnected` as props from App.jsx (D-08). Do not change the visuals; note the CHAT status boolean now means session-acked (this is wired at the App.jsx level in 04-07, StatusBar just renders whatever boolean it receives). Create `TerminalLine.jsx` (default export `TerminalLine({ line })`, lines 483-536; import `renderPayload` from `frontend/lib/format` if used rather than redefining). Create `TerminalInput.jsx` (default export, lines 538-608 — keep the `fetch('/terminal/exec', {method:'POST'})` call and its inline error-state handling verbatim; RESEARCH.md Pitfall 5 — do not stub the route; passing `session_id` as an extra body field is Claude's discretion per CONTEXT.md, not required). Create `TerminalPanel.jsx` (default export, lines 610-673 — keep the `bottomRef`/`scrollIntoView` auto-scroll idiom; import and compose `TerminalLine` and `TerminalInput` from their new files). Import icons from `lucide-react`. Preserve all `panel`/`panel-header`/`input-field` classes (04-UI-SPEC). All `.jsx` (D-06). Do NOT edit App.jsx.
  </action>
  <verify>
    <automated>cd frontend && node -e "['StatusBar','TerminalLine','TerminalInput','TerminalPanel'].forEach(n=>{const s=require('fs').readFileSync('components/'+n+'.jsx','utf8'); if(!new RegExp('export default').test(s))throw new Error('no default export '+n)}); const tp=require('fs').readFileSync('components/TerminalPanel.jsx','utf8'); if(!/TerminalLine/.test(tp)||!/TerminalInput/.test(tp))throw new Error('TerminalPanel does not compose its sub-parts'); const ti=require('fs').readFileSync('components/TerminalInput.jsx','utf8'); if(!/terminal\\/exec/.test(ti))throw new Error('terminal exec call not preserved'); console.log('batch2a OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `StatusBar.jsx` default-exports `StatusBar` and co-locates `EngagementTimer`; renders three connection indicators from props
    - `TerminalLine.jsx`, `TerminalInput.jsx`, `TerminalPanel.jsx` each default-export their function
    - `TerminalPanel.jsx` imports and composes `TerminalLine` and `TerminalInput`
    - `TerminalInput.jsx` preserves the `/terminal/exec` POST + inline error state verbatim (no stub, no removal)
    - App.jsx is NOT modified
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract FindingsPanel (report download) + smoke tests for the batch</name>
  <files>frontend/components/FindingsPanel.jsx, frontend/components/panels-batch2.smoke.test.jsx</files>
  <read_first>
    - frontend/src/App.jsx (FindingsPanel lines 675-872, including triggerDownload/downloadReport 682-717)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the FindingsPanel section — the file-download pattern given verbatim)
    - frontend/lib/constants.js (SEVERITY_MAP, REPORT_FORMATS_UI, REPORT_FRAMEWORKS to import)
    - frontend/src/App.test.jsx (React Testing Library render idioms for the smoke test)
    - .planning/phases/04-frontend-split/04-UI-SPEC.md (severity badge colors, panel shape to preserve)
  </read_first>
  <action>
    Create `FindingsPanel.jsx` (default export) by moving App.jsx lines 675-872 verbatim, including the `triggerDownload`/`downloadReport` blob-download flow (682-717) unchanged — keep the `fetch('/report/...', {method:'POST'})` calls and blob handling exactly (RESEARCH.md Pitfall 5 — route absence is pre-existing/out-of-scope, do not stub). Import `SEVERITY_MAP`, `REPORT_FORMATS_UI`, `REPORT_FRAMEWORKS` from `frontend/lib/constants` instead of redefining, and any formatter from `frontend/lib/format`. Preserve severity badge classes and panel shape (04-UI-SPEC). Then create `frontend/components/panels-batch2.smoke.test.jsx`: import and shallow-render StatusBar (with `chatConnected={false} eventsConnected={false} terminalConnected={false}` and any other required props defaulted), TerminalPanel (empty lines), TerminalInput, TerminalLine (minimal `line`), and FindingsPanel (`findings={[]}` and any callback props stubbed), asserting each renders without throwing and produces its expected root element. Use `@testing-library/react` `render`. Satisfies VALIDATION.md's UI-02 standalone-file test line for this batch.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run components/panels-batch2.smoke.test.jsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `FindingsPanel.jsx` default-exports `FindingsPanel` and preserves the `triggerDownload`/`downloadReport` blob flow and `/report/*` POST verbatim
    - `FindingsPanel` imports `SEVERITY_MAP`/`REPORT_FORMATS_UI`/`REPORT_FRAMEWORKS` from `frontend/lib/constants`
    - `panels-batch2.smoke.test.jsx` imports and renders all batch-2 components without throwing
    - `npx vitest run components/panels-batch2.smoke.test.jsx` passes
    - App.jsx is NOT modified
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel-rendered server data → DOM | Findings/terminal/status data from the backend is rendered |
| Report blob → local file download | FindingsPanel writes a server-provided blob to a downloaded file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02d | Tampering / XSS | Extracted panel render paths | mitigate | Verbatim extraction preserves React JSX escaping; no `dangerouslySetInnerHTML` in source and none introduced. `TerminalLine` renders command output as escaped JSX text. |
| T-04-06 | Information Disclosure | FindingsPanel report download | accept | Blob is created from a server response the operator explicitly requested and downloaded to their own machine (single-operator, CLAUDE.md); no new exposure. The `/report/*` route does not exist in the current backend (Pitfall 5) — call preserved as-is. |
| T-04-05b | (out-of-scope backend absence) | /terminal/exec, /report/* REST calls | accept | Routes absent in current backend — pre-existing, out of Phase 4 scope; preserved verbatim, documented as expected non-functional. |
| T-04-SC | Tampering | No new package installs | accept | Uses only already-approved react/lucide-react/testing-library from 04-01's audited manifest. |
</threat_model>

<verification>
- `npx vitest run components/panels-batch2.smoke.test.jsx` green
- StatusBar/TerminalLine/TerminalInput/TerminalPanel/FindingsPanel all exist as standalone default-export files
- TerminalPanel composes its co-located sub-parts; FindingsPanel preserves the blob-download flow
- No REST/WS call was stubbed, removed, or altered
</verification>

<success_criteria>
StatusBar (with EngagementTimer), the Terminal trio, and FindingsPanel exist as standalone importable files with verbatim-preserved behavior and styling, smoke-tested — completing the panel-extraction half of UI-02 and readying 04-07's ErrorBoundary wrapping.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-split/04-06-SUMMARY.md` when done.
</output>
