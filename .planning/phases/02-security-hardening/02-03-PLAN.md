---
phase: 02-security-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/memory/client_profile.py
  - backend/intelligence/research_kb.py
  - tests/memory/test_client_profile.py
  - tests/intelligence/test_research_kb_wal.py
autonomous: true
requirements: [DATA-01]
user_setup: []

must_haves:
  truths:
    - "ClientProfileDB reports journal_mode=wal after initialize() (D-06)"
    - "ResearchKB reports journal_mode=wal after initialize() (D-06)"
    - "Both connections apply PRAGMA synchronous=NORMAL immediately after connecting"
    - "session_store.py is not modified in this phase — real session persistence stays Phase 3's PERSIST-01 (D-07)"
  artifacts:
    - path: "backend/memory/client_profile.py"
      provides: "WAL pragma on connection"
      contains: "PRAGMA journal_mode=WAL"
    - path: "backend/intelligence/research_kb.py"
      provides: "WAL pragma on connection"
      contains: "PRAGMA journal_mode=WAL"
    - path: "tests/intelligence/test_research_kb_wal.py"
      provides: "DATA-01 test for ResearchKB WAL mode"
      contains: "journal_mode"
  key_links:
    - from: "backend/memory/client_profile.py"
      to: "sqlite connection"
      via: "PRAGMA applied after connect, before executescript"
      pattern: "journal_mode=WAL"
    - from: "backend/intelligence/research_kb.py"
      to: "sqlite connection"
      via: "PRAGMA applied after connect, before executescript"
      pattern: "journal_mode=WAL"
---

<objective>
Apply `PRAGMA journal_mode=WAL; PRAGMA synchronous=NORMAL;` immediately after each `sqlite3.connect()` in `ClientProfileDB` and `ResearchKB`, satisfying DATA-01. Both classes open exactly one connection per process, so reissuing the per-connection `synchronous` pragma once per process is correct.

Purpose: Make concurrent SQLite writes durable and non-corrupting. WAL allows concurrent readers with a single writer.
Output: Two 3-line pragma insertions plus tests proving `journal_mode` reads back as `wal`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-hardening/02-CONTEXT.md
@.planning/phases/02-security-hardening/02-RESEARCH.md
@.planning/phases/02-security-hardening/02-PATTERNS.md

<interfaces>
<!-- Both DB classes share an identical initialize() shape (from client_profile.py:51-59 / research_kb.py:53-60): -->
<!--   self._conn = await asyncio.to_thread(sqlite3.connect, str(self._db_path), check_same_thread=False) -->
<!--   self._conn.row_factory = sqlite3.Row -->
<!--   await asyncio.to_thread(self._conn.executescript, "CREATE TABLE ...") -->
<!-- Insert the two PRAGMA lines AFTER row_factory and BEFORE executescript, in BOTH files. -->
<!-- Both constructors accept db_path (ClientProfileDB(db_path=...), ResearchKB(db_path=...)); tests use tmp_path. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WAL tests (extend ClientProfile, new ResearchKB WAL test)</name>
  <files>tests/memory/test_client_profile.py, tests/intelligence/test_research_kb_wal.py</files>
  <read_first>
    - tests/memory/test_client_profile.py (existing tmp_path fixture and async test pattern — extend it)
    - backend/memory/client_profile.py (initialize() and constructor db_path param)
    - backend/intelligence/research_kb.py (initialize() and constructor db_path param)
    - .planning/phases/02-security-hardening/02-PATTERNS.md (Dataclass-driven test fixtures with tmp_path shared pattern)
  </read_first>
  <action>
    Extend tests/memory/test_client_profile.py with an async test that constructs `ClientProfileDB(db_path=tmp_path / "wal.db")`, calls `await db.initialize()`, then queries `PRAGMA journal_mode;` back (via `await asyncio.to_thread(db._conn.execute, "PRAGMA journal_mode")` fetchone) and asserts the value lowercased equals `"wal"`. Create tests/intelligence/test_research_kb_wal.py mirroring the same tmp_path real-file pattern for `ResearchKB(db_path=tmp_path / "kb.db")` — initialize, query `PRAGMA journal_mode;`, assert `"wal"`. (No new `tests/intelligence/__init__.py` needed — the directory is already a package.) These tests are expected to FAIL until Task 2 (RED).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/intelligence/test_research_kb_wal.py').read()); ast.parse(open('tests/memory/test_client_profile.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - tests/intelligence/test_research_kb_wal.py exists and parses
    - Both test files query `PRAGMA journal_mode` and assert the result equals `wal` (case-insensitive)
    - `grep -c "journal_mode" tests/intelligence/test_research_kb_wal.py` returns >= 1
    - Uses `tmp_path` real-file connections (not mocks)
  </acceptance_criteria>
  <done>WAL assertion tests exist for both ClientProfileDB and ResearchKB using real temp-file connections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply WAL + synchronous pragmas in both DB classes</name>
  <files>backend/memory/client_profile.py, backend/intelligence/research_kb.py</files>
  <read_first>
    - backend/memory/client_profile.py (initialize() lines 51-59)
    - backend/intelligence/research_kb.py (initialize() lines 53-60)
    - .planning/phases/02-security-hardening/02-RESEARCH.md (Pattern 3 — exact pragma insertion; Pitfall 4 — synchronous does not persist, must reissue per connection)
    - .planning/phases/02-security-hardening/02-PATTERNS.md (client_profile/research_kb section — 3-line insertion applies verbatim in each)
  </read_first>
  <behavior>
    - Test: ClientProfileDB after initialize() returns "wal" for PRAGMA journal_mode
    - Test: ResearchKB after initialize() returns "wal" for PRAGMA journal_mode
  </behavior>
  <action>
    In both backend/memory/client_profile.py and backend/intelligence/research_kb.py, immediately after `self._conn.row_factory = sqlite3.Row` and before the `executescript` CREATE TABLE call, insert two statements: `await asyncio.to_thread(self._conn.execute, "PRAGMA journal_mode=WAL")` and `await asyncio.to_thread(self._conn.execute, "PRAGMA synchronous=NORMAL")`. Add a one-line comment noting that `journal_mode` persists in the DB file but `synchronous` does NOT and must be reissued on every new connection (RESEARCH.md Pitfall 4). The insertion is identical in both files. Do not change `check_same_thread=False` or any table DDL.
  </action>
  <verify>
    <automated>pytest tests/memory/test_client_profile.py tests/intelligence/test_research_kb_wal.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PRAGMA journal_mode=WAL" backend/memory/client_profile.py` returns 1
    - `grep -c "PRAGMA synchronous=NORMAL" backend/memory/client_profile.py` returns 1
    - `grep -c "PRAGMA journal_mode=WAL" backend/intelligence/research_kb.py` returns 1
    - `grep -c "PRAGMA synchronous=NORMAL" backend/intelligence/research_kb.py` returns 1
    - Pragmas appear AFTER `row_factory` and BEFORE `executescript` in both files
    - `pytest tests/memory/test_client_profile.py tests/intelligence/test_research_kb_wal.py -x` passes
  </acceptance_criteria>
  <done>Both DB classes apply WAL + synchronous=NORMAL on connect; journal_mode reads back as wal; DATA-01 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent writers → shared SQLite file | Multiple async writers to the same DB file; WAL governs concurrent read/write safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering / Denial of Service | Concurrent SQLite writers corrupting or lock-contending on a shared DB file (ClientProfileDB, ResearchKB) | mitigate | `PRAGMA journal_mode=WAL` (concurrent readers + single writer, durable) and `PRAGMA synchronous=NORMAL` applied on connect (DATA-01). Note: WAL reduces, does not eliminate, write serialization — acceptable at personal-use single-operator write volume |
| T-02-08 | Denial of Service | Future new-connection code path silently reverting `synchronous` to driver default | accept | Both classes open one connection per process today; a code comment warns future contributors that `synchronous` must be reissued per connection (Pitfall 4) — no runtime enforcement needed this phase |
</threat_model>

<verification>
- `pytest tests/memory/test_client_profile.py tests/intelligence/test_research_kb_wal.py -x` passes
- `pytest` full suite green — no regression
- grep gates in acceptance_criteria all pass
</verification>

<success_criteria>
- Both `ClientProfileDB` and `ResearchKB` report `journal_mode=wal` after `initialize()` (DATA-01 / ROADMAP success criterion #3)
- `synchronous=NORMAL` applied on the single per-process connection in each
</success_criteria>

<output>
Create `.planning/phases/02-security-hardening/02-03-SUMMARY.md` when done
</output>
