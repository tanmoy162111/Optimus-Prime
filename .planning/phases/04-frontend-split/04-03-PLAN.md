---
phase: 04-frontend-split
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/components/ErrorBoundary.jsx
  - frontend/components/ErrorBoundary.test.jsx
  - frontend/context/SessionContext.jsx
  - frontend/context/SessionContext.test.jsx
autonomous: true
requirements: [UI-02, UI-03]
user_setup: []

must_haves:
  truths:
    - "An ErrorBoundary catches a render error thrown by a child and renders the locked fallback markup without unmounting siblings (UI-02)"
    - "The ErrorBoundary 'Reset panel' action re-mounts the boundary's children (retry), not a page reload"
    - "A component nested under SessionContext.Provider reads session state via useContext without prop-drilling (UI-03)"
    - "The SessionContext value is memoized — an unrelated App.jsx re-render does not create a new context object reference"
  artifacts:
    - path: "frontend/components/ErrorBoundary.jsx"
      provides: "Class-based error boundary with UI-SPEC-locked fallback markup"
      contains: "getDerivedStateFromError"
    - path: "frontend/context/SessionContext.jsx"
      provides: "SessionContext + useSession hook"
      exports: ["SessionContext", "useSession"]
    - path: "frontend/components/ErrorBoundary.test.jsx"
      provides: "UI-02 fault-isolation test"
    - path: "frontend/context/SessionContext.test.jsx"
      provides: "UI-03 context-access + memoization test"
  key_links:
    - from: "frontend/components/ErrorBoundary.jsx"
      to: "sibling panels"
      via: "componentDidCatch isolates crash to boundary subtree"
      pattern: "getDerivedStateFromError"
    - from: "frontend/context/SessionContext.jsx"
      to: "consuming components"
      via: "useContext(SessionContext)"
      pattern: "useContext"
---

<objective>
Create the two genuinely net-new modules of this phase (no in-repo analog): the class-based `ErrorBoundary` (UI-02, D-05) that wraps every extracted panel for fault isolation, and `SessionContext` (UI-03, D-07/D-08) that distributes session state without prop-drilling. Both are small, fully specified in RESEARCH.md (Patterns 1 and 2) and 04-UI-SPEC.md (locked fallback markup). This plan builds them test-first since both have clear, assertable behavior.

Purpose: Provide the fault-isolation and state-distribution primitives that 04-07 wires into App.jsx's render tree.
Output: `frontend/components/ErrorBoundary.jsx`, `frontend/context/SessionContext.jsx`, plus their Vitest test files (the Wave-0 test coverage VALIDATION.md requires for UI-02 and UI-03).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-frontend-split/04-CONTEXT.md
@.planning/phases/04-frontend-split/04-PATTERNS.md
@.planning/phases/04-frontend-split/04-RESEARCH.md
@.planning/phases/04-frontend-split/04-UI-SPEC.md

<interfaces>
<!-- Both modules are fully specified verbatim in the referenced docs. -->
ErrorBoundary: default-export class component. Props: `panelName` (string, shown in fallback header), `children`. State: `{ hasError, resetKey }`. Uses static getDerivedStateFromError + componentDidCatch (console.error only — never render error.message/stack). Fallback markup and copy locked in 04-UI-SPEC.md "New UI Surface: Error Boundary Fallback".
SessionContext: `export const SessionContext = createContext(null)`; `export const useSession = () => useContext(SessionContext)`. The Provider value (mirrored from App.jsx useState, wired in 04-07) carries: sessionId, chatConnected, eventsConnected, terminalConnected, scope, currentPlan, engagementActive, engagementStart.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ErrorBoundary class component with locked fallback (test-first)</name>
  <files>frontend/components/ErrorBoundary.jsx, frontend/components/ErrorBoundary.test.jsx</files>
  <read_first>
    - .planning/phases/04-frontend-split/04-RESEARCH.md (Pattern 1: ErrorBoundary — the full class implementation, verified React-canonical)
    - .planning/phases/04-frontend-split/04-UI-SPEC.md ("New UI Surface: Error Boundary Fallback (UI-02)" — the fallback markup and copy are LOCKED verbatim here)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the `frontend/components/ErrorBoundary.jsx` section)
    - frontend/src/App.test.jsx (lines 12-38 — the MockWebSocket + React Testing Library test idioms already used in this repo, for consistency)
  </read_first>
  <behavior>
    - Test 1: Rendering a child that throws during render inside `<ErrorBoundary panelName="Terminal">` shows the fallback text "This panel crashed" and does not propagate the throw past the boundary.
    - Test 2: A sibling `<ErrorBoundary panelName="Findings"><div>OK sibling</div></ErrorBoundary>` rendered next to a crashing boundary still renders "OK sibling" (fault isolation — one crash does not unmount siblings).
    - Test 3: The fallback header shows `{panelName} — Error` (e.g. "Terminal — Error").
    - Test 4: The fallback renders the "Reset panel" button; clicking it clears `hasError` and re-mounts children (bumping `resetKey`), so a now-non-throwing child renders normally.
    - Test 5: The fallback never contains the thrown error's message text (no stack/message leakage — Information Disclosure control).
  </behavior>
  <action>
    Write `frontend/components/ErrorBoundary.test.jsx` FIRST, encoding the five behaviors above using `@testing-library/react` (`render`, `screen`, `fireEvent`/`user-event`). Use a small throwing test component (a component that throws in render when a prop flag is set) to trigger the boundary. Run the suite and confirm it FAILS (RED). Then create `frontend/components/ErrorBoundary.jsx` exactly per RESEARCH.md Pattern 1 / 04-PATTERNS.md: a default-export class extending `Component`, `state = { hasError: false, resetKey: 0 }`, `static getDerivedStateFromError()` returning `{ hasError: true }`, `componentDidCatch(error, info)` logging via `console.error(\`[ErrorBoundary:${this.props.panelName}]\`, error, info)` ONLY (never render error.message/stack — Information Disclosure control per RESEARCH.md Security Domain), a `handleReset` that bumps `resetKey` and clears `hasError`, and a `render()` returning the LOCKED fallback markup from 04-UI-SPEC.md verbatim (panel/panel-header shape, AlertTriangle icons at sizes 13 and 22, copy "This panel crashed" / "An unexpected error stopped rendering. Other panels are unaffected." / "Reset panel" button with RefreshCw icon). Wrap children as `<div key={this.state.resetKey}>{this.props.children}</div>` so Reset re-mounts (fresh instance), not merely re-renders. Import `Component` from `react` and `AlertTriangle, RefreshCw` from `lucide-react`. Run the suite again and confirm GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run components/ErrorBoundary.test.jsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run components/ErrorBoundary.test.jsx` passes all five behaviors
    - `frontend/components/ErrorBoundary.jsx` default-exports a class with `static getDerivedStateFromError` and `componentDidCatch`
    - The fallback markup contains the exact strings "This panel crashed", "An unexpected error stopped rendering. Other panels are unaffected.", and "Reset panel"
    - `componentDidCatch` calls `console.error` and the render output does NOT include `error.message` or `error.stack`
    - Reset re-mounts children via a `key` bound to `resetKey`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SessionContext provider + useSession hook (test-first)</name>
  <files>frontend/context/SessionContext.jsx, frontend/context/SessionContext.test.jsx</files>
  <read_first>
    - .planning/phases/04-frontend-split/04-RESEARCH.md (Pattern 2: SessionContext mirror — createContext + useMemo Provider, and the "inline object literal" pitfall)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the `frontend/context/SessionContext.jsx` section)
    - .planning/phases/04-frontend-split/04-CONTEXT.md (D-07 field list, D-08 mirror decision)
  </read_first>
  <behavior>
    - Test 1: A consumer component calling `useSession()` inside a `<SessionContext.Provider value={{...}}>` reads the provided fields (e.g. `sessionId`, `chatConnected`) without receiving them as props.
    - Test 2: `useSession()` returns `null` (the createContext default) when rendered with no Provider — proving the default is `null`, not undefined.
    - Test 3 (memoization): Given a test host that mirrors App.jsx's pattern — `useState` values wrapped in `useMemo` over the exact D-07 field list, passed to the Provider — a consumer wrapped so it can count renders does NOT re-render when the host re-renders without changing any of the memoized fields (proves the value object reference is stable across unrelated re-renders).
  </behavior>
  <action>
    Write `frontend/context/SessionContext.test.jsx` FIRST encoding the three behaviors above. For Test 3, build a small test host component that holds the eight D-07 fields (`sessionId, chatConnected, eventsConnected, terminalConnected, scope, currentPlan, engagementActive, engagementStart`) and computes the Provider value via `useMemo(() => ({...}), [<each field individually>])`, then trigger an unrelated re-render (e.g. an extra `useState` counter bumped by a button) and assert the consumer's render count did not increase — mirroring RESEARCH.md Pattern 2's memoization guarantee. Run and confirm RED. Then create `frontend/context/SessionContext.jsx` per RESEARCH.md Pattern 2 verbatim: `import { createContext, useContext } from 'react'`, `export const SessionContext = createContext(null)`, `export const useSession = () => useContext(SessionContext)`. (The Provider itself lives in App.jsx per D-08 — this module only defines the context and the hook; the memoization is a usage contract this test enforces so 04-07 wires it correctly.) Run and confirm GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run context/SessionContext.test.jsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run context/SessionContext.test.jsx` passes all three behaviors
    - `frontend/context/SessionContext.jsx` exports `SessionContext` (created with default `null`) and `useSession`
    - The memoization test proves a stable context value reference across unrelated re-renders (consumer render count does not increase)
    - `useSession()` returns `null` when no Provider is present
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component render error → DOM fallback | A crashing panel's error details must not leak into the rendered UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | ErrorBoundary fallback | mitigate | The locked UI-SPEC fallback copy renders only generic text ("This panel crashed" / generic body); `componentDidCatch` logs the actual error via `console.error` only and the render path never emits `error.message`/`error.stack`. Test 5 asserts the error message is absent from the fallback DOM. |
| T-04-04 | Denial of Service (single-panel crash) | ErrorBoundary isolation | mitigate | Each panel is individually wrapped (D-05); `getDerivedStateFromError` contains the crash to the boundary subtree so one panel failure cannot white-screen the dashboard. Test 2 asserts sibling survival. |
| T-04-SC | Tampering | No new package installs | accept | Uses only already-approved `react` + `lucide-react` + testing-library from 04-01's audited manifest. |
</threat_model>

<verification>
- `npx vitest run components/ErrorBoundary.test.jsx context/SessionContext.test.jsx` green
- ErrorBoundary fallback markup matches 04-UI-SPEC.md verbatim and leaks no error text
- SessionContext value is memoization-stable per the UI-03 test
</verification>

<success_criteria>
UI-02's fault-isolation primitive and UI-03's prop-drilling-free state distribution primitive both exist, are test-covered, and are ready for 04-07 to wire into App.jsx's render tree.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-split/04-03-SUMMARY.md` when done.
</output>
