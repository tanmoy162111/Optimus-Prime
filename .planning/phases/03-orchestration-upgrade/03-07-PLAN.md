---
phase: 03-orchestration-upgrade
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/instruction_parser.py
  - tests/agent/test_instruction_parser.py
autonomous: true
requirements: [ORCH-03]
must_haves:
  truths:
    - "instruction_parser.py no longer defines a duplicate EngineRouter class — it imports the canonical one from engine_router.py"
    - "InstructionParser.parse() accepts an EngagementSession (not the old SessionState) and no longer reads a nonexistent session.mode field"
    - "parse()'s returned 'mode' is derived from EngineRouter().dispatch(intent, target), not from a session attribute"
  artifacts:
    - path: "backend/agent/instruction_parser.py"
      provides: "reconciled InstructionParser with canonical EngineRouter import and EngagementSession signature"
      contains: "from backend.agent.engine_router import EngineRouter"
  key_links:
    - from: "backend/agent/instruction_parser.py"
      to: "backend.agent.engine_router.EngineRouter"
      via: "import + inline dispatch for mode derivation"
      pattern: "EngineRouter\\(\\)\\.dispatch"
---

<objective>
Reconcile instruction_parser.py per D-02: delete its byte-identical duplicate `EngineRouter` class (import the canonical one from engine_router.py), change `InstructionParser.parse()`'s signature from the old `SessionState` to the live `EngagementSession`, and re-derive the returned `mode` via `EngineRouter().dispatch(intent, target)` since `EngagementSession` has no `.mode` field.

Purpose: RESEARCH.md Pattern 3 confirmed (via full-repo grep) the duplicate is byte-identical and `InstructionParser.parse()` has ZERO live callers — this reconciliation is safe with no call-site regression. The `mode` re-derivation is the one genuine design decision (not a mechanical rename), making parse() the merge point for the "route to engine" step OmO's Executor loop (D-02) needs. This has zero test coverage today.
Output: reconciled instruction_parser.py + a new test module covering the signature change and mode-derivation merge.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-orchestration-upgrade/03-RESEARCH.md
@.planning/phases/03-orchestration-upgrade/03-PATTERNS.md

<interfaces>
Current instruction_parser.py: imports `from backend.agent.conversation import SessionState`; defines a local `EngineRouter` (lines 11-36, byte-identical to engine_router.py); `InstructionParser.parse(message, session: SessionState, mode=None)` returns dict with `"mode": mode or session.mode`. Helper methods _detect_intent, _extract_target, _extract_constraints, _detect_phase, _calculate_confidence (lines 61-146) are unchanged.

Canonical EngineRouter (backend/agent/engine_router.py): `dispatch(intent, target=None) -> str` returns one of "MLAIEngine"/"ICSEngine"/"InfrastructureEngine".

EngagementSession (backend/session/engagement_session.py): has NO `.mode` field. The closest analog is EngineRouter.dispatch()'s per-message return value.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dedupe EngineRouter + retype parse() to EngagementSession + derive mode</name>
  <files>backend/agent/instruction_parser.py, tests/agent/test_instruction_parser.py</files>
  <read_first>
    - backend/agent/instruction_parser.py (full current file — the duplicate EngineRouter and the session.mode read at line 58)
    - backend/agent/engine_router.py (the canonical EngineRouter to import)
    - backend/session/engagement_session.py (confirm EngagementSession has no .mode field)
    - .planning/phases/03-orchestration-upgrade/03-RESEARCH.md (Pattern 3 — the exact before/after diff and the mode-derivation design note)
  </read_first>
  <behavior>
    - Test: parse("recon acme.com", session=EngagementSession.create()) returns a dict with keys intent/target/constraints/phase/confidence/mode and does not raise (no session.mode AttributeError)
    - Test: with an explicit mode="ICSEngine" argument, the returned "mode" equals "ICSEngine"
    - Test: with mode=None, the returned "mode" equals EngineRouter().dispatch(intent, target) for the parsed intent/target (e.g. an ML model target like "model.onnx" yields "MLAIEngine")
    - Test: instruction_parser module no longer defines an EngineRouter class (it imports it)
  </behavior>
  <action>
    In instruction_parser.py: replace `from backend.agent.conversation import SessionState` with `from backend.session.engagement_session import EngagementSession` and `from backend.agent.engine_router import EngineRouter`. Delete the local `EngineRouter` class (lines 11-36) entirely — it is byte-identical to the canonical one (verified). Change `parse(self, message, session: EngagementSession, mode: Optional[str] = None)`. Replace the return dict's `"mode": mode or session.mode` with `"mode": mode or EngineRouter().dispatch(intent, target)` (EngagementSession has no .mode field; this is the RESEARCH.md-flagged genuine merge point, not a rename). Keep all helper methods (_detect_intent, _extract_target, _extract_constraints, _detect_phase, _calculate_confidence) byte-identical. Create tests/agent/test_instruction_parser.py (plain pytest, no fixture, mirroring test_llm_router.py style) covering the behaviors above.
  </action>
  <verify>
    <automated>pytest tests/agent/test_instruction_parser.py -x --tb=short && python -c "import ast,inspect; import backend.agent.instruction_parser as m; assert not hasattr(m,'EngineRouter') or m.EngineRouter.__module__=='backend.agent.engine_router'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `instruction_parser.py` imports EngineRouter from `backend.agent.engine_router` and defines no local EngineRouter class body
    - `InstructionParser.parse()` is annotated with `EngagementSession` and no longer references `session.mode` (grep for `session.mode` in the file returns nothing)
    - parse() with mode=None derives mode via `EngineRouter().dispatch(intent, target)`
    - `from backend.agent.conversation import SessionState` no longer appears in the file
    - New test module green; the full existing suite still imports instruction_parser without error
  </acceptance_criteria>
  <done>Duplicate EngineRouter removed, parse() retyped to EngagementSession, mode derived via canonical dispatch, tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator message → parsed intent/target | Regex extraction of intent/target/constraints from operator text (trusted operator) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | duplicate EngineRouter drift | mitigate | Deleting the duplicate removes the risk of the two copies diverging silently; single canonical dispatch source |
| T-03-SC | Tampering | package installs | accept | No package installs this phase |
</threat_model>

<verification>
- `pytest tests/agent/test_instruction_parser.py -x --tb=short` green
- `python -c "import backend.agent.instruction_parser, backend.agent.orchestrator"` imports clean (orchestrator imports InstructionParser)
</verification>

<success_criteria>
- Single canonical EngineRouter; InstructionParser.parse() accepts EngagementSession; mode derived, not read from a missing field
</success_criteria>

<output>
Create `.planning/phases/03-orchestration-upgrade/03-07-SUMMARY.md` when done
</output>
