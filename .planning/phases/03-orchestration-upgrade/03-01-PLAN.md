---
phase: 03-orchestration-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/memory/smart_memory.py
  - tests/agent/test_conversation_summariser.py
  - tests/memory/test_smart_memory.py
autonomous: true
requirements: [ORCH-02]
must_haves:
  truths:
    - "Instantiating ConversationSummariser() no longer raises AttributeError"
    - "Calling SmartMemory.get_best_tools() returns [] instead of raising AttributeError"
    - "config.settings exposes qwen_model, deepseek_api_key, deepseek_model, deepseek_base_url, summariser_threshold"
  artifacts:
    - path: "backend/config.py"
      provides: "New Settings fields for multi-provider routing and summariser threshold"
      contains: "qwen_model"
    - path: "backend/memory/smart_memory.py"
      provides: "get_best_tools() no-op stub"
      contains: "def get_best_tools"
  key_links:
    - from: "backend/agent/conversation_summariser.py"
      to: "backend.config.settings.summariser_threshold"
      via: "attribute read at __init__"
      pattern: "summariser_threshold"
---

<objective>
Fix the two pre-existing defects RESEARCH.md confirmed this phase's own code depends on, and add the config fields ORCH-02 multi-provider routing requires — all before any consuming code (LLMRouter compaction route, OmO Architect-role stub, context compaction) is built.

Purpose: `ConversationSummariser()` and `StrategyEvolutionEngine.enrich_chain()` both raise `AttributeError` today (RESEARCH.md Pitfalls 1 and 2). `config.py` also lacks the `qwen_model`/`deepseek_*` fields ORCH-02/D-04 needs. These are prerequisite fixes with zero dependencies — this plan is Wave 1 so every downstream plan can rely on them.
Output: `config.py` with 5 new Settings fields, a `SmartMemory.get_best_tools()` stub, and two smoke/regression tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-orchestration-upgrade/03-RESEARCH.md
@.planning/phases/03-orchestration-upgrade/03-PATTERNS.md

<interfaces>
Existing Settings field style (backend/config.py — no Field() wrapper used anywhere in the class):
  class Settings(BaseSettings):
      bearer_token: str = "dev-token"
      claude_model: str = "claude-sonnet-4-6"
      mistral_model: str = "mistral:7b"
      ...
      model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}

Existing SmartMemory degrade idiom (backend/memory/smart_memory.py):
  async def search(self, query: str, top_k: int = 5) -> List[MemoryEntry]:
      return self.entries[-top_k:]   # "return what little I have, never raise"

ConversationSummariser reads settings.summariser_threshold at __init__ (backend/agent/conversation_summariser.py line ~12).
StrategyEvolutionEngine._enrich_node() calls await self._memory.get_best_tools(target_type=..., top_k=10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multi-provider + summariser Settings fields to config.py</name>
  <files>backend/config.py, tests/agent/test_conversation_summariser.py</files>
  <read_first>
    - backend/config.py (see the exact existing field style; do NOT introduce Field() wrappers)
    - backend/agent/conversation_summariser.py (confirm the summariser_threshold read site)
    - tests/agent/test_llm_router.py (mirror the test_claude_model_is_sonnet_4_6 regression-test shape)
  </read_first>
  <action>
    Add these five fields to the `Settings` class in backend/config.py, using the existing `name: type = default` style (no `Field(...)`), placed alongside the other model fields and before `model_config`:
    `summariser_threshold: int = 60000` (RESEARCH.md Pitfall 1 — currently missing, breaks ConversationSummariser),
    `qwen_model: str = "qwen2.5:7b"` (RESEARCH.md Assumption A1 — [ASSUMED] default; the operator confirms the real Ollama tag via .env),
    `deepseek_api_key: str = ""`,
    `deepseek_model: str = "deepseek-chat"`,
    `deepseek_base_url: str = "https://api.deepseek.com"`.
    Do not change any existing field. These serve ORCH-02/D-04 (qwen_model, deepseek_*) and Pitfall 1 (summariser_threshold).
  </action>
  <verify>
    <automated>python -c "from backend import config; s=config.settings; assert s.summariser_threshold==60000 and s.qwen_model and s.deepseek_model=='deepseek-chat' and s.deepseek_base_url and s.deepseek_api_key=='' ; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from backend import config; print(config.settings.summariser_threshold)"` prints `60000`
    - `config.settings.qwen_model`, `config.settings.deepseek_model`, `config.settings.deepseek_base_url` are all non-empty strings
    - `config.settings.deepseek_api_key` defaults to empty string (so its absence never breaks orchestration/compaction per D-04)
    - No existing Settings field was renamed or removed (grep confirms `claude_model` and `mistral_model` still present)
  </acceptance_criteria>
  <done>All five fields present on Settings with the specified defaults; import-time smoke check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add SmartMemory.get_best_tools() no-op stub</name>
  <files>backend/memory/smart_memory.py, tests/memory/test_smart_memory.py</files>
  <read_first>
    - backend/memory/smart_memory.py (replicate the existing async method style and the search() degrade idiom)
    - backend/intelligence/strategy_evolution.py (confirm the call signature: get_best_tools(target_type=..., top_k=10))
    - tests/memory/test_smart_memory.py (extend the existing test module; do not rewrite xfailed tests)
  </read_first>
  <action>
    Add an async method `get_best_tools(self, target_type: str, top_k: int = 10) -> List[Dict[str, Any]]` to the `SmartMemory` class in backend/memory/smart_memory.py that returns `[]` (matching search()'s "return what little I have, never raise" idiom; D-10 minimal-stub bar, RESEARCH.md Assumption A3 — return `[]`, NOT `NotImplementedError`). Import `Any` from typing if not already imported. This makes `StrategyEvolutionEngine._enrich_node()`'s existing call safe without editing strategy_evolution.py.
    Add a test to tests/memory/test_smart_memory.py asserting `await SmartMemory(...).get_best_tools("web", top_k=10) == []` and that the call does not raise.
  </action>
  <verify>
    <automated>pytest tests/memory/test_smart_memory.py -k get_best_tools -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `SmartMemory` has an async `get_best_tools` method (grep `def get_best_tools` returns one hit)
    - The new test asserting `get_best_tools(...) == []` passes and does not raise `AttributeError`/`NotImplementedError`
    - No existing SmartMemory method was modified
  </acceptance_criteria>
  <done>get_best_tools() returns [] without raising; new test green.</done>
</task>

<task type="auto">
  <name>Task 3: ConversationSummariser instantiation smoke test</name>
  <files>tests/agent/test_conversation_summariser.py</files>
  <read_first>
    - backend/agent/conversation_summariser.py (confirm __init__ reads settings.summariser_threshold)
    - tests/agent/test_llm_router.py (plain-pytest smoke-test shape, no fixture)
  </read_first>
  <action>
    Create tests/agent/test_conversation_summariser.py with a smoke test that instantiates `ConversationSummariser()` and asserts it does not raise `AttributeError` (this is the regression guard for RESEARCH.md Pitfall 1 — the AttributeError only disappears because Task 1 added `summariser_threshold`). Assert the instance's threshold attribute equals `config.settings.summariser_threshold` (60000). Keep it a plain pytest function, no fixture, following test_llm_router.py's style.
  </action>
  <verify>
    <automated>pytest tests/agent/test_conversation_summariser.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `ConversationSummariser()` instantiates without raising in the test
    - Test asserts the summariser's threshold reads 60000 from config
    - Test file exists at tests/agent/test_conversation_summariser.py
  </acceptance_criteria>
  <done>Smoke test green; ConversationSummariser is instantiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → config.settings | Operator-controlled config values load into the process (trusted single operator) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | deepseek_api_key in config | mitigate | Defaults to empty string; never logged; presence-gated per D-04 so absence is a no-op |
| T-03-SC | Tampering | npm/pip installs | accept | No package installs this phase (RESEARCH.md Package Legitimacy Audit — zero new packages); nothing to verify |
</threat_model>

<verification>
- `pytest tests/agent/test_conversation_summariser.py tests/memory/test_smart_memory.py -x --tb=short` green
- Import smoke check for all five new config fields passes
</verification>

<success_criteria>
- config.settings exposes summariser_threshold, qwen_model, deepseek_api_key, deepseek_model, deepseek_base_url
- ConversationSummariser() and SmartMemory.get_best_tools() no longer raise AttributeError
</success_criteria>

<output>
Create `.planning/phases/03-orchestration-upgrade/03-01-SUMMARY.md` when done
</output>
