---
phase: 04-frontend-split
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/components/ScopePanel.jsx
  - frontend/components/DirectivesPanel.jsx
  - frontend/components/PlanPanel.jsx
  - frontend/components/AgentTracker.jsx
  - frontend/components/HealthPanel.jsx
  - frontend/components/panels-batch1.smoke.test.jsx
autonomous: true
requirements: [UI-02]
user_setup: []

must_haves:
  truths:
    - "ScopePanel, DirectivesPanel, PlanPanel, AgentTracker, and HealthPanel each exist as standalone importable files under frontend/components/ (UI-02)"
    - "Each extracted panel renders its established panel/panel-header shape and preserves its existing props/callbacks and REST/behavior verbatim"
  artifacts:
    - path: "frontend/components/ScopePanel.jsx"
      provides: "Scope config form (REST POST /scope via onSetScope)"
    - path: "frontend/components/DirectivesPanel.jsx"
      provides: "Directive trigger chips (onSendDirective)"
    - path: "frontend/components/PlanPanel.jsx"
      provides: "Engagement plan renderer with empty state"
    - path: "frontend/components/AgentTracker.jsx"
      provides: "Agent status tracker"
    - path: "frontend/components/HealthPanel.jsx"
      provides: "Health status panel (onRefresh GET /health)"
    - path: "frontend/components/panels-batch1.smoke.test.jsx"
      provides: "Import + shallow-render smoke tests for the five panels"
  key_links:
    - from: "frontend/components/ScopePanel.jsx"
      to: "backend /scope (via onSetScope prop)"
      via: "callback prop wired from App.jsx handleSetScope"
      pattern: "onSetScope"
---

<objective>
Extract five inline sub-components from `App.jsx` into standalone files under `frontend/components/` (D-04 full componentization): `ScopePanel`, `DirectivesPanel`, `PlanPanel`, `AgentTracker`, `HealthPanel`. These are the transform/event-driven/request-response panels — pure verbatim extractions (the PATTERNS map rates all five "exact"). Only the module boundary (imports/exports/props) changes; JSX and logic are copied unchanged. Note (RESEARCH.md Pitfall 5): several of these call backend routes that do not exist in the current backend (`/scope`, `/directives`, `/report/*`) — their non-functional appearance after extraction is a PRE-EXISTING, out-of-scope condition, NOT a regression this plan introduces. Preserve the calls exactly as they are today.

Purpose: Deliver half of UI-02's "each panel is a standalone file" requirement (the other panels are 04-06); enables 04-07 to wrap each in an ErrorBoundary.
Output: Five new panel files + one smoke test file. Does NOT edit App.jsx (that swap is 04-07).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-frontend-split/04-CONTEXT.md
@.planning/phases/04-frontend-split/04-PATTERNS.md
@.planning/phases/04-frontend-split/04-UI-SPEC.md
@frontend/lib/constants.js
@frontend/lib/format.js

<interfaces>
<!-- Exact App.jsx line ranges to extract verbatim (from 04-PATTERNS.md, verified against a 1505-line App.jsx). Props stay as callbacks wired from App.jsx. -->
ScopePanel({ scope, onSetScope })      — App.jsx lines 265-375 (local form state + REST POST via onSetScope)
DirectivesPanel({ onSendDirective })   — App.jsx lines 377-420 (local `icons` map + directive chips)
PlanPanel({ plan })                    — App.jsx lines 929-1013 (includes early-return empty state 930-944)
AgentTracker({ agents })               — App.jsx lines 874-927
HealthPanel({ health, onRefresh })     — App.jsx lines 1181-1221
Shared imports available: frontend/lib/constants.js (SEVERITY_MAP, EVENT_ICONS, REPORT_FORMATS_UI, REPORT_FRAMEWORKS), frontend/lib/format.js (fmtTime, fmtElapsed, renderPayload), lucide-react icons.
Panel shape convention: <div className="panel flex flex-col h-full"><div className="panel-header">…</div>{content}</div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ScopePanel, DirectivesPanel, and HealthPanel (verbatim)</name>
  <files>frontend/components/ScopePanel.jsx, frontend/components/DirectivesPanel.jsx, frontend/components/HealthPanel.jsx</files>
  <read_first>
    - frontend/src/App.jsx (ScopePanel lines 265-375; DirectivesPanel lines 377-420; HealthPanel lines 1181-1221)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the ScopePanel / DirectivesPanel / HealthPanel sections)
    - frontend/lib/constants.js and frontend/lib/format.js (import any SEVERITY_MAP/EVENT_ICONS/fmtTime these panels reference, instead of redefining)
    - .planning/phases/04-frontend-split/04-UI-SPEC.md (panel shape, color, and spacing to preserve verbatim)
  </read_first>
  <action>
    Create three files, each a default-export function moved verbatim from the cited App.jsx line ranges: `ScopePanel({ scope, onSetScope })` (lines 265-375 — keep the local `useState` form fields and the `onSetScope` callback that calls `fetch('/scope', {method:'POST'})` in App.jsx; do NOT stub or change the fetch — RESEARCH.md Pitfall 5, the route's absence is pre-existing/out-of-scope), `DirectivesPanel({ onSendDirective })` (lines 377-420 — keep the local `icons` map and directive chips), `HealthPanel({ health, onRefresh })` (lines 1181-1221). For each: add `import { useState } from 'react'` where the source uses it, import needed icons from `lucide-react`, and import any shared constant/formatter from `frontend/lib/constants` or `frontend/lib/format` rather than duplicating it. Keep the exact `panel`/`panel-header`/`label-xs`/`input-field`/`btn-primary`/`btn-ghost` classes (04-UI-SPEC — no parallel styling, no inline style color values). Files stay `.jsx` (D-06). Do NOT edit App.jsx.
  </action>
  <verify>
    <automated>cd frontend && node -e "['ScopePanel','DirectivesPanel','HealthPanel'].forEach(n=>{const s=require('fs').readFileSync('components/'+n+'.jsx','utf8'); if(!new RegExp('export default function '+n).test(s))throw new Error('no default export '+n); if(!/className=\"panel/.test(s))throw new Error('panel shape missing '+n)}); console.log('batch1a OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ScopePanel.jsx`, `DirectivesPanel.jsx`, `HealthPanel.jsx` each default-export their named function with the exact prop signature above
    - Each preserves the `panel flex flex-col h-full` + `panel-header` shape
    - `ScopePanel` still calls the `/scope` POST via `onSetScope` (unchanged); no fetch was stubbed or removed
    - Shared constants/formatters are imported from `frontend/lib/*`, not redefined
    - App.jsx is NOT modified
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract PlanPanel and AgentTracker (verbatim) + smoke tests for the batch</name>
  <files>frontend/components/PlanPanel.jsx, frontend/components/AgentTracker.jsx, frontend/components/panels-batch1.smoke.test.jsx</files>
  <read_first>
    - frontend/src/App.jsx (PlanPanel lines 929-1013 including empty state 930-944; AgentTracker lines 874-927)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the PlanPanel / AgentTracker sections)
    - frontend/src/App.test.jsx (React Testing Library render idioms to mirror in the smoke test)
    - .planning/phases/04-frontend-split/04-UI-SPEC.md (PlanPanel's sky-bordered plan card styling to preserve)
  </read_first>
  <action>
    Create `PlanPanel({ plan })` (App.jsx lines 929-1013 verbatim, including the early-return empty state at 930-944) and `AgentTracker({ agents })` (lines 874-927 verbatim). Import icons from `lucide-react` and any shared constant/formatter from `frontend/lib/*`. Then create `frontend/components/panels-batch1.smoke.test.jsx`: for each of the five panels in this plan (ScopePanel, DirectivesPanel, HealthPanel, PlanPanel, AgentTracker), a smoke test that imports the component and shallow-renders it with minimal/empty props (e.g. `PlanPanel plan={null}` hits the empty state; `AgentTracker agents={[]}`; `HealthPanel health={null} onRefresh={()=>{}}`; `ScopePanel scope={{}} onSetScope={()=>{}}`; `DirectivesPanel onSendDirective={()=>{}}`) and asserts each renders without throwing and produces the `panel` container. Use `@testing-library/react` `render` + `container.querySelector('.panel')`. This satisfies VALIDATION.md's UI-02 "standalone files, independently importable" test line.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run components/panels-batch1.smoke.test.jsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `PlanPanel.jsx` (default-export `PlanPanel({ plan })`, includes the empty-state early return) and `AgentTracker.jsx` (default-export `AgentTracker({ agents })`) exist
    - `panels-batch1.smoke.test.jsx` imports and renders all five batch-1 panels without throwing, asserting a `.panel` element per panel
    - `npx vitest run components/panels-batch1.smoke.test.jsx` passes
    - App.jsx is NOT modified
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel-rendered server data → DOM | Panels render backend-provided scope/plan/agent/health data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02c | Tampering / XSS | Extracted panel render paths | mitigate | Verbatim extraction preserves React JSX text-node escaping; no `dangerouslySetInnerHTML` exists in the source and none is introduced. Panels render server data as escaped JSX. |
| T-04-05 | (out-of-scope backend absence) | ScopePanel/DirectivesPanel/HealthPanel REST calls | accept | `/scope`, `/directives` etc. do not exist in the current backend (RESEARCH.md Pitfall 5) — pre-existing, out of Phase 4 scope. Calls are preserved verbatim; their non-functional appearance is documented, not a regression. |
| T-04-SC | Tampering | No new package installs | accept | Uses only already-approved react/lucide-react/testing-library from 04-01's audited manifest. |
</threat_model>

<verification>
- `npx vitest run components/panels-batch1.smoke.test.jsx` green
- All five files exist as standalone default-export components with preserved panel shape
- No fetch call was stubbed, removed, or altered (extraction is a refactor, not a backend fix)
</verification>

<success_criteria>
Five panels (ScopePanel, DirectivesPanel, PlanPanel, AgentTracker, HealthPanel) exist as standalone, independently importable files with verbatim-preserved behavior and styling, smoke-tested, ready for 04-07 to wrap in ErrorBoundary and render — advancing UI-02.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-split/04-05-SUMMARY.md` when done.
</output>
