---
phase: 03-orchestration-upgrade
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/omx.py
  - tests/agent/test_omx.py
autonomous: true
requirements: [ORCH-03]
must_haves:
  truths:
    - "OmX.plan() returns a Pydantic-validated EngagementPlan DAG for a canonical $pentest-style directive (D-01: full OmX reconstruction)"
    - "OmX uses Claude forced tool use (tool_choice type=tool, disable_parallel_tool_use=True) — not free-text parsing, and not rule/template-based matching (D-06: LLM-driven plan generation)"
    - "On repeated ValidationError, OmX retries up to 3 total attempts then raises OmXPlanValidationError — it never returns a partial/default plan"
    - "OmX reuses the existing LLMRouter Claude client and does NOT fall back to Ollama for the structured call"
  artifacts:
    - path: "backend/agent/omx.py"
      provides: "Directive/EngagementPlan Pydantic models, OmX.plan(), OmXPlanValidationError"
      contains: "class EngagementPlan"
    - path: "tests/agent/test_omx.py"
      provides: "plan-generation, validation-retry, and 3-attempt-failure tests"
      contains: "def test_plan_generates_valid_dag"
  key_links:
    - from: "backend/agent/omx.py"
      to: "llm_router.claude.messages.create"
      via: "forced tool use tool_choice"
      pattern: "tool_choice"
---

<objective>
Build OmX (D-01 layer 1, D-06, ORCH-03): the LLM-driven workflow planner that decomposes an operator directive into a Pydantic-validated `EngagementPlan` DAG of directives via Claude forced tool use, with a 3-attempt validation-retry loop and a hard failure (`OmXPlanValidationError`) rather than dispatching a malformed/hallucinated plan.

Purpose: This closes Critical Failure Mode #3 (malformed/hallucinated DAG dispatched as if valid). AI-SPEC.md Section 3/4b fully specifies the schema, forced-tool-use pattern, retry loop, and prompt discipline — this plan implements them verbatim. OmX operates as a component fully independent of OmO (traceable as a separate concern per ROADMAP success criterion 3).
Output: omx.py with Directive/EngagementPlan models, plan(), OmXPlanValidationError, and tests using mocked Claude responses.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-orchestration-upgrade/03-AI-SPEC.md
@.planning/phases/03-orchestration-upgrade/03-RESEARCH.md
@.planning/phases/03-orchestration-upgrade/03-PATTERNS.md
@OPTIMUS_PRIME_ARCHITECTURE.md

<interfaces>
LLMRouter (backend/agent/llm_router.py) — self.claude is anthropic.AsyncAnthropic already constructed; self.claude_model / config.settings.claude_model is the model id. OmX reuses llm_router.claude directly (forced tool use needs raw SDK access complete() doesn't expose) — do NOT instantiate a second Anthropic client.

AI-SPEC Section 3 plan() shape (ground truth — implement verbatim):
  class Directive(BaseModel): id; phase; engine: Literal["InfrastructureEngine","MLAIEngine","ICSEngine"]; agent: str; target: str; tools: list[str]=[]; depends_on: list[str]=[]; gate_required: bool=False
  class EngagementPlan(BaseModel): directives: list[Directive]; rationale: str
  plan tool input_schema = EngagementPlan.model_json_schema()
  await self.llm_router.claude.messages.create(model=..., max_tokens=2048, system=_OMX_PLANNING_SYSTEM_PROMPT, messages=..., tools=[plan_tool], tool_choice={"type":"tool","name":"emit_engagement_plan","disable_parallel_tool_use":True})
  tool_use = next(b for b in response.content if b.type=="tool_use")   # NEVER index response.content[0]
  EngagementPlan.model_validate(tool_use.input) — on ValidationError, append assistant content + corrective user turn, retry (3 attempts total), else raise OmXPlanValidationError

EngagementSession.scope: ScopeConfig(targets, exclusions, stealth_level, ports, protocols).
The 7 planning keywords ($pentest, $cloud-audit, $genai-probe, $recon, $scope-discover, $iam-audit, $endpoint) — OPTIMUS_PRIME_ARCHITECTURE.md §3.1. Embed the canonical $pentest 8-phase decomposition as a static few-shot in _OMX_PLANNING_SYSTEM_PROMPT.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Directive/EngagementPlan models + OmXPlanValidationError + system prompt</name>
  <files>backend/agent/omx.py, tests/agent/test_omx.py</files>
  <read_first>
    - .planning/phases/03-orchestration-upgrade/03-AI-SPEC.md (Section 3 Entry Point Pattern + Section 4b Structured Outputs — the verbatim schema and prompt discipline)
    - backend/agent/sub_agents/base.py (ToolPermissionError minimal-exception idiom for OmXPlanValidationError)
    - OPTIMUS_PRIME_ARCHITECTURE.md §3.1 (the 7 planning keywords and $pentest 8-phase decomposition)
    - backend/session/engagement_session.py (ScopeConfig fields to inject into the system prompt as hard constraints)
  </read_first>
  <behavior>
    - Test: EngagementPlan.model_validate(a well-formed dict with 8 directives) succeeds and exposes 8 Directive objects
    - Test: a directive with engine="BogusEngine" fails Pydantic validation (Literal constraint)
    - Test: _OMX_PLANNING_SYSTEM_PROMPT is a distinct constant from orchestrator._SYSTEM_PROMPT and contains the closed engine vocabulary and a $pentest few-shot
  </behavior>
  <action>
    Create backend/agent/omx.py with: module `logger`, `class OmXPlanValidationError(Exception): pass` (minimal idiom), the `Directive` and `EngagementPlan` Pydantic BaseModels exactly per AI-SPEC Section 3 (engine as `Literal["InfrastructureEngine","MLAIEngine","ICSEngine"]`, gate_required defaulting False, depends_on/tools defaulting empty), and a dedicated `_OMX_PLANNING_SYSTEM_PROMPT` constant (do NOT reuse orchestrator._SYSTEM_PROMPT per AI-SPEC Section 4b). The system prompt must state the closed engine vocabulary and registered agent names as constraints, instruct that every directive.target must be a member of the operator's approved scope.targets and not in scope.exclusions (setting gate_required=True for ambiguous/discovered assets rather than silently including or excluding — Scope fidelity dimension), instruct that tool/flag selection must respect scope.stealth_level on EVERY attempt including retries (Known Failure Mode #2), and embed the canonical $pentest 8-phase decomposition as a static inline few-shot example. Do not add a retrieval/vector step (7 fixed keywords — inline few-shot only).
  </action>
  <verify>
    <automated>pytest tests/agent/test_omx.py -k "model or schema or prompt" -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `EngagementPlan.model_validate({...8 directives...})` returns an object with 8 directives
    - A directive with an invalid `engine` value raises `pydantic.ValidationError`
    - `OmXPlanValidationError` subclasses Exception with no custom __init__
    - `_OMX_PLANNING_SYSTEM_PROMPT` is defined in omx.py, references the three valid engine names, and is not the same string as orchestrator._SYSTEM_PROMPT
  </acceptance_criteria>
  <done>Schema + error type + planning system prompt defined and unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OmX.plan() forced-tool-use call with 3-attempt validation retry</name>
  <files>backend/agent/omx.py, tests/agent/test_omx.py</files>
  <read_first>
    - .planning/phases/03-orchestration-upgrade/03-AI-SPEC.md (Section 3 plan() method — implement verbatim; Section 4b retry logic + max_tokens sizing)
    - backend/agent/llm_router.py (self.claude client, claude_model; the Claude call shape and why OmX must NOT copy the Ollama fallback)
    - tests/agent/test_llm_router.py (Claude AsyncMock pattern for tests — mock messages.create to return a content list with a tool_use block)
  </read_first>
  <behavior>
    - Test test_plan_generates_valid_dag: mock claude.messages.create to return a response whose content has a tool_use block with valid EngagementPlan input → plan() returns a validated EngagementPlan
    - Test: filtering picks the tool_use block by .type=="tool_use" even when content[0] is a text block (never index content[0] — AI-SPEC pitfall #1)
    - Test: mock returns an INVALID tool_use.input twice then a VALID one → plan() succeeds on attempt 3 and messages.create was called 3 times
    - Test test_three_failed_attempts_raises: mock returns invalid input all 3 times → plan() raises OmXPlanValidationError and messages.create was called exactly 3 times
    - Test: a response with stop_reason=="max_tokens" is treated as a validation failure (triggers retry), not parsed as complete
  </behavior>
  <action>
    Add `OmX.__init__(self, llm_router)` that stores llm_router and builds `self._plan_tool = {"name":"emit_engagement_plan","description":...,"input_schema": EngagementPlan.model_json_schema()}` (derive the schema from the model so it cannot drift). Add `async def plan(self, directive_text, session) -> EngagementPlan` implementing AI-SPEC Section 3 verbatim: loop up to 3 attempts calling `self.llm_router.claude.messages.create(model=config.settings.claude_model, max_tokens=2048, system=_OMX_PLANNING_SYSTEM_PROMPT, messages=messages, tools=[self._plan_tool], tool_choice={"type":"tool","name":"emit_engagement_plan","disable_parallel_tool_use":True})`. Select the tool_use block via `next(b for b in response.content if b.type=="tool_use")` (never `content[0]`). Treat `response.stop_reason=="max_tokens"` as a truncated-JSON validation failure. On `ValidationError` (or truncation), log at WARNING with the attempt number and error text (NEVER log raw target/credentials at INFO+ — match CredentialVault "never logged" discipline), append the model's own content plus a corrective user turn ("Your plan failed validation: {e}. Return a corrected plan."), and retry. After 3 failed attempts raise `OmXPlanValidationError`. Do NOT copy LLMRouter's Claude→Ollama fallback — Ollama cannot produce a tool_use block (RESEARCH.md Environment Availability); OmX's only failure path is retry-then-raise. Keep the whole method a plain `async def` awaited by callers (never asyncio.run — AI-SPEC pitfall #2).
  </action>
  <verify>
    <automated>pytest tests/agent/test_omx.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `plan()` returns a validated EngagementPlan when Claude returns a well-formed tool_use block
    - `plan()` selects the tool_use block by type, not by position (test with a leading text block passes)
    - 3 consecutive validation failures raise `OmXPlanValidationError` with exactly 3 messages.create calls
    - A max_tokens stop_reason triggers the retry path
    - No branch in plan() calls Ollama or LLMRouter.complete() (grep confirms omx.py does not reference _ollama or mode="orchestration" fallback)
  </acceptance_criteria>
  <done>plan() produces validated DAGs, retries on validation failure, and hard-fails after 3 attempts; all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator directive text → OmX prompt | Untrusted-ish free text from the (trusted) operator becomes the user turn |
| Claude tool_use output → EngagementPlan | LLM-generated structure crosses into execution-planning state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | OmX plan generation | mitigate | Pydantic EngagementPlan validation of every tool_use.input; engine constrained to Literal; 3-attempt retry then OmXPlanValidationError — never dispatch a partial/default plan |
| T-03-01 | Elevation of Privilege | OmX scope inference | mitigate (partial) | _OMX_PLANNING_SYSTEM_PROMPT states scope.targets/exclusions/stealth_level as hard constraints and instructs gate_required=True for ambiguous assets; deterministic set-membership enforcement lands in OmO (Plan 08) — the prompt reduces but does not eliminate over-reach (AI-SPEC pitfall #3) |
| T-03-06 | Tampering | prompt injection via directive text | accept | Single trusted operator; directive text is the operator's own; documented low-severity per phase threat guidance |
| T-03-SC | Tampering | package installs | accept | No package installs this phase (anthropic/pydantic already pinned) |
</threat_model>

<verification>
- `pytest tests/agent/test_omx.py -x --tb=short` green
- `python -c "from backend.agent.omx import OmX, EngagementPlan, Directive, OmXPlanValidationError"` imports clean
</verification>

<success_criteria>
- OmX generates validated EngagementPlan DAGs via forced tool use
- Malformed/hallucinated plans fail validation and raise after 3 attempts, never dispatch
- OmX is an independent component (no OmO import)
</success_criteria>

<output>
Create `.planning/phases/03-orchestration-upgrade/03-06-SUMMARY.md` when done
</output>
