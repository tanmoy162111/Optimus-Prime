---
phase: 02-security-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/verification/verification_loop.py
  - tests/verification/__init__.py
  - tests/verification/test_verification_loop.py
autonomous: true
requirements: [DATA-02]
user_setup: []

must_haves:
  truths:
    - "VerificationLoop tracks request counts keyed by f'{engagement_id}:{finding_id}'"
    - "Two engagements verifying the same finding_id keep independent, non-shared budgets"
    - "check_and_increment returns False once VerificationPolicy.max_requests_per_finding is exceeded"
    - "VerificationLoop consumes VerificationPolicy as an injected constructor dependency, not reimplementing limits"
  artifacts:
    - path: "backend/verification/verification_loop.py"
      provides: "engagement-scoped verification budget stub"
      contains: "def check_and_increment"
      min_lines: 25
    - path: "tests/verification/test_verification_loop.py"
      provides: "DATA-02 test for per-engagement budget isolation"
      contains: "engagement"
  key_links:
    - from: "backend/verification/verification_loop.py"
      to: "backend/verification/verification_policy.py"
      via: "constructor injection of VerificationPolicy / DEFAULT_VERIFICATION_POLICY"
      pattern: "VerificationPolicy"
    - from: "backend/verification/verification_loop.py"
      to: "_request_counts dict"
      via: "keyed by engagement_id:finding_id"
      pattern: "f\"\\{engagement_id\\}:\\{finding_id\\}\""
---

<objective>
Create a minimal, forward-compatible `VerificationLoop` stub whose `_request_counts` dict is keyed by `f"{engagement_id}:{finding_id}"`, satisfying DATA-02. The prior (deleted) implementation keyed by bare `finding_id` — that IS the cross-engagement budget-bleed bug this phase fixes. This stub is scoping-only: no CONFIRMED/FALSE_POSITIVE/MANUAL_REVIEW classification (deferred to v1.1 Phase 5 per D-09).

Purpose: Ensure concurrent engagements verifying the same finding_id do not share or exhaust each other's request budget.
Output: `backend/verification/verification_loop.py` (new) consuming `VerificationPolicy`, plus a unit test proving per-engagement counter isolation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-hardening/02-CONTEXT.md
@.planning/phases/02-security-hardening/02-RESEARCH.md
@.planning/phases/02-security-hardening/02-PATTERNS.md

<interfaces>
<!-- backend/verification/verification_policy.py provides: -->
<!--   class VerificationPolicy (frozen dataclass) with max_requests_per_finding: int = 3 -->
<!--   DEFAULT_VERIFICATION_POLICY = VerificationPolicy()  (module-level singleton) -->
<!-- Target stub interface (RESEARCH.md Pattern 4 / PATTERNS.md verification_loop section): -->
<!--   VerificationLoop(policy: VerificationPolicy | None = None) -->
<!--   .policy -> VerificationPolicy (property) -->
<!--   ._key(engagement_id, finding_id) -> "f{engagement_id}:{finding_id}" -->
<!--   .check_and_increment(engagement_id, finding_id) -> bool  (True while within budget) -->
<!-- Return bool, NOT a FindingClassification enum (that enum belongs to v1.1 Phase 5, does not exist here). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DATA-02 budget-isolation unit test (Wave 0 scaffold)</name>
  <files>tests/verification/__init__.py, tests/verification/test_verification_loop.py</files>
  <read_first>
    - backend/verification/verification_policy.py (VerificationPolicy, max_requests_per_finding=3, DEFAULT_VERIFICATION_POLICY)
    - tests/memory/test_client_profile.py (dataclass-driven unit test structure reference)
    - .planning/phases/02-security-hardening/02-RESEARCH.md (Pattern 4 — target interface; the bug being fixed)
    - .planning/phases/02-security-hardening/02-PATTERNS.md (verification_loop section — bug provenance from git history)
  </read_first>
  <action>
    Create tests/verification/__init__.py (empty package marker) and tests/verification/test_verification_loop.py. Tests: (1) with default policy (max_requests_per_finding=3), calling `check_and_increment("eng-A", "F1")` three times returns True, True, True and the fourth returns False; (2) isolation — call `check_and_increment("eng-A", "F1")` to exhaustion, then assert `check_and_increment("eng-B", "F1")` (same finding_id, different engagement) still returns True, proving budgets do not bleed across engagements; (3) a custom `VerificationPolicy(max_requests_per_finding=1)` injected into the constructor is honored (second call returns False). These tests are expected to FAIL (module does not exist yet) until Task 2 (RED).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/verification/test_verification_loop.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - tests/verification/__init__.py exists
    - tests/verification/test_verification_loop.py parses as valid Python
    - Contains the cross-engagement isolation test (same finding_id, engagements eng-A and eng-B, independent budgets)
    - Contains a custom-policy injection test (max_requests_per_finding=1)
    - `grep -c "engagement" tests/verification/test_verification_loop.py` returns >= 1
  </acceptance_criteria>
  <done>Unit test asserts budget exhaustion at the policy limit and per-engagement isolation for a shared finding_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the VerificationLoop scoping stub</name>
  <files>backend/verification/verification_loop.py</files>
  <read_first>
    - backend/verification/verification_policy.py (constructor dependency — VerificationPolicy, DEFAULT_VERIFICATION_POLICY, max_requests_per_finding)
    - .planning/phases/02-security-hardening/02-RESEARCH.md (Pattern 4 — the fully specified stub)
    - .planning/phases/02-security-hardening/02-PATTERNS.md (verification_loop section — module docstring convention, constructor-injection idiom, cite §7.4 N9)
  </read_first>
  <behavior>
    - Test: three check_and_increment calls for one (engagement, finding) return True/True/True then the 4th returns False (default policy limit 3)
    - Test: same finding_id under a different engagement_id has an independent budget
    - Test: an injected VerificationPolicy(max_requests_per_finding=1) is honored
  </behavior>
  <action>
    Create backend/verification/verification_loop.py per RESEARCH.md Pattern 4. Module docstring: one-line summary, cite architecture §7.4 (N9), and explicitly state this is a scoping-only stub that excludes CONFIRMED/FALSE_POSITIVE/MANUAL_REVIEW classification (v1.1 Phase 5 scope, per D-09). Import `VerificationPolicy` and `DEFAULT_VERIFICATION_POLICY` from `backend.verification.verification_policy`. Class `VerificationLoop`: `__init__(self, policy: VerificationPolicy | None = None)` storing `self._policy = policy or DEFAULT_VERIFICATION_POLICY` and `self._request_counts: Dict[str, int] = {}`; a `policy` property; `_key(self, engagement_id, finding_id)` returning `f"{engagement_id}:{finding_id}"`; and `check_and_increment(self, engagement_id, finding_id) -> bool` that increments the per-key count and returns `count <= self._policy.max_requests_per_finding`. Keep `engagement_id` and `finding_id` as SEPARATE arguments (not a pre-built key) so v1.1 Phase 5 can add a `verify_finding(...)` wrapper without changing this signature. Do NOT import anything from the deleted `backend.core` module tree, and do NOT add classification logic.
  </action>
  <verify>
    <automated>pytest tests/verification/test_verification_loop.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - backend/verification/verification_loop.py exists with class `VerificationLoop`
    - `grep -q "def check_and_increment" backend/verification/verification_loop.py` succeeds
    - `grep -q 'f"{engagement_id}:{finding_id}"' backend/verification/verification_loop.py` succeeds
    - Imports `VerificationPolicy` / `DEFAULT_VERIFICATION_POLICY` from backend.verification.verification_policy
    - No import from `backend.core` (`grep -c "backend.core" backend/verification/verification_loop.py` returns 0)
    - No CONFIRMED/FALSE_POSITIVE/MANUAL_REVIEW classification logic present
    - `pytest tests/verification/test_verification_loop.py -x` passes
  </acceptance_criteria>
  <done>VerificationLoop stub tracks per-(engagement, finding) budgets against the injected policy; cross-engagement isolation holds; DATA-02 test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engagement A verification budget ↔ engagement B verification budget | Shared in-memory counter dict was the bleed vector; per-engagement key namespacing is the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure / Tampering | Cross-engagement verification-budget bleed — the prior impl keyed `_request_counts` by bare `finding_id`, so concurrent engagements shared/exhausted one budget | mitigate | Key `_request_counts` by `f"{engagement_id}:{finding_id}"` so each engagement's counter is independent (DATA-02) |
| T-02-10 | Tampering | Runtime mutation of verification limits bypassing the policy | mitigate | `VerificationPolicy` is a frozen dataclass injected as a constructor dependency; the stub reads `max_requests_per_finding` from it rather than reimplementing/mutating the limit |
</threat_model>

<verification>
- `pytest tests/verification/test_verification_loop.py -x` passes
- `pytest` full suite green — no regression
- grep gates in acceptance_criteria all pass
</verification>

<success_criteria>
- `check_and_increment(engagement_id, finding_id)` enforces `VerificationPolicy.max_requests_per_finding` per `{engagement_id}:{finding_id}` key (DATA-02 / ROADMAP success criterion #4)
- Two engagements verifying the same finding_id keep independent budgets
- Stub excludes classification logic (v1.1 Phase 5 scope preserved)
</success_criteria>

<output>
Create `.planning/phases/02-security-hardening/02-04-SUMMARY.md` when done
</output>
