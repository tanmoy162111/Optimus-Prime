---
phase: 03-orchestration-upgrade
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/agent/llm_router.py
  - tests/agent/test_llm_router.py
autonomous: true
requirements: [ORCH-02]
must_haves:
  truths:
    - "LLMRouter.complete(mode='orchestration') still resolves to Claude (regression guard — Claude never removed) (D-00: multi-provider, not a Claude replacement)"
    - "LLMRouter.complete(mode='compaction') resolves to Ollama/Qwen (config.settings.qwen_model), never Claude (D-04)"
    - "A DeepSeek provider route exists, gated on config.settings.deepseek_api_key presence, so its absence never breaks orchestration or compaction (D-04)"
  artifacts:
    - path: "backend/agent/llm_router.py"
      provides: "task-based multi-provider routing in complete()"
      contains: "compaction"
  key_links:
    - from: "backend/agent/llm_router.py"
      to: "config.settings.qwen_model"
      via: "mode='compaction' Ollama generate call"
      pattern: "qwen_model"
---

<objective>
Extend LLMRouter for ORCH-02/D-04 task-based multi-provider routing: keep Claude for `mode="orchestration"` (unchanged, never removed), add `mode="compaction"` → Ollama/Qwen, and wire an optional DeepSeek provider gated on API-key presence. This is NOT a Claude replacement (D-00/D-04).

Purpose: ROADMAP success criterion 2 requires task-based routing across Claude + Ollama + at least one additional provider (DeepSeek), with Claude remaining fully supported. Plan 01 already added the required config fields (qwen_model, deepseek_*). RESEARCH.md State-of-the-Art warns: the existing Claude→Ollama fallback-on-error path is DISTINCT from the new compaction routing — do not conflate them; both remain after this phase.
Output: extended complete() dispatcher + tests asserting per-mode provider resolution and the DeepSeek key-gate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-orchestration-upgrade/03-RESEARCH.md
@.planning/phases/03-orchestration-upgrade/03-PATTERNS.md

<interfaces>
Current LLMRouter (backend/agent/llm_router.py):
  __init__: self.claude = anthropic.AsyncAnthropic(...); self.ollama = OllamaClient(config.settings.ollama_host)
  async def complete(self, messages, mode="orchestration", system="") -> LLMResponse:
      if mode == "orchestration": return await self._claude_complete(messages, system)
      return await self._ollama_complete(messages)              # everything else → Ollama (mistral_model)
  _claude_complete: try Claude; except Exception: log "falling back to Ollama"; return _ollama_complete(messages)   # ERROR fallback — distinct from routing
  _ollama_complete: self.ollama.generate(model=config.settings.mistral_model, prompt=...)

New config fields (from Plan 01): qwen_model, deepseek_api_key, deepseek_model, deepseek_base_url.
LLMResponse dataclass: content, model_used, input_tokens, output_tokens.
DeepSeek assumed OpenAI-compatible (RESEARCH.md Assumption A2) — callable via httpx/aiohttp, no new SDK; lower-risk because D-04 makes it optional.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mode='compaction' → Ollama/Qwen routing</name>
  <files>backend/agent/llm_router.py, tests/agent/test_llm_router.py</files>
  <read_first>
    - backend/agent/llm_router.py (complete() dispatcher + _ollama_complete shape)
    - backend/inference/ollama_client.py (generate() signature)
    - tests/agent/test_llm_router.py (existing Claude/Ollama mock patterns and the mode assertions)
  </read_first>
  <behavior>
    - Test test_compaction_mode_routes_to_ollama: complete(messages, mode="compaction") calls the Ollama client with model=config.settings.qwen_model and NEVER calls claude.messages.create
    - Test test_orchestration_mode_still_claude (regression guard): complete(messages, mode="orchestration") calls claude.messages.create
    - Test: an unknown/default mode still routes to the existing Ollama path (unchanged behavior)
  </behavior>
  <action>
    Extend `complete()` in llm_router.py with an explicit `elif mode == "compaction":` branch that calls a new `_compaction_complete(messages)` helper. `_compaction_complete` reuses the existing `self.ollama` client (do NOT construct a second OllamaClient) and calls `self.ollama.generate(model=config.settings.qwen_model, prompt=...)` using the same prompt-join shape as `_ollama_complete`, returning an LLMResponse with model_used=config.settings.qwen_model. Keep the existing `if mode == "orchestration"` Claude branch and the final Ollama fallback for other modes byte-identical. Do NOT touch `_claude_complete`'s except-fallback-to-Ollama behavior — that error path is separate from routing (RESEARCH.md State-of-the-Art). Extend tests/agent/test_llm_router.py with the compaction-routing and orchestration-regression tests using mocked clients.
  </action>
  <verify>
    <automated>pytest tests/agent/test_llm_router.py::test_compaction_mode_routes_to_ollama tests/agent/test_llm_router.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `complete(mode="compaction")` resolves to Ollama with `config.settings.qwen_model` and does not call Claude (asserted via mock)
    - `complete(mode="orchestration")` still calls Claude (existing tests + new regression test pass)
    - No second OllamaClient is constructed (grep confirms one `OllamaClient(` in the module, in __init__)
  </acceptance_criteria>
  <done>Compaction routes to Qwen; orchestration still Claude; existing behavior preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Optional DeepSeek provider route, gated on API-key presence</name>
  <files>backend/agent/llm_router.py, tests/agent/test_llm_router.py</files>
  <read_first>
    - backend/agent/llm_router.py (complete() dispatcher after Task 1)
    - backend/config.py (deepseek_api_key / deepseek_model / deepseek_base_url fields from Plan 01)
    - .planning/phases/03-orchestration-upgrade/03-RESEARCH.md (Assumption A2 — DeepSeek OpenAI-compatible; A2 is a checkpoint-worthy assumption, not a blocker since DeepSeek is optional)
  </read_first>
  <behavior>
    - Test: complete(messages, mode="deepseek") with config.settings.deepseek_api_key=="" (default) does NOT attempt a DeepSeek HTTP call and falls back gracefully (routes to Ollama or returns a logged no-op LLMResponse) — absence never breaks
    - Test: with a non-empty deepseek_api_key (monkeypatched) and a mocked HTTP client, mode="deepseek" issues a request to config.settings.deepseek_base_url with the configured model
    - Test: mode="orchestration" and mode="compaction" are unaffected by the DeepSeek branch
  </behavior>
  <action>
    Add a `mode == "deepseek"` branch to `complete()` calling a new `_deepseek_complete(messages)` helper. The helper first checks `if not config.settings.deepseek_api_key:` and, when empty, logs a WARNING and degrades gracefully (route to `self._ollama_complete(messages)`) so DeepSeek's absence never breaks orchestration/compaction (D-04). When a key is present, issue an OpenAI-compatible chat-completions POST to `config.settings.deepseek_base_url` with `config.settings.deepseek_model` using a lightweight `httpx`/`aiohttp` client (already pinned deps — no new SDK, RESEARCH.md Assumption A2), wrapping the call in try/except that degrades to Ollama on error (mirror the _claude_complete except-fallback shape). Return an LLMResponse. Extend tests with both the key-absent (no HTTP attempt) and key-present (mocked HTTP) paths.
  </action>
  <verify>
    <automated>pytest tests/agent/test_llm_router.py -k "deepseek" -x --tb=short && pytest tests/agent/test_llm_router.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - With empty `deepseek_api_key`, `complete(mode="deepseek")` makes no outbound DeepSeek HTTP call and returns without raising
    - With a monkeypatched key + mocked HTTP client, the DeepSeek branch targets `config.settings.deepseek_base_url` and `deepseek_model`
    - orchestration and compaction routing are unchanged by this task (their tests still pass)
    - No new package appears in backend/requirements.txt (httpx/aiohttp already present)
  </acceptance_criteria>
  <done>DeepSeek wired as an optional, key-gated provider that degrades safely when unconfigured; full router suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → external LLM providers | Prompts leave the process to Claude / Ollama(local) / DeepSeek(remote) |
| deepseek_api_key → outbound request | Secret used to authenticate the optional provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | compaction prompt content to Ollama | mitigate | Compaction routes to local Ollama/Qwen (no cloud egress); only summariser-shaped content per AI-SPEC dimension 8 — no raw target/credential beyond what conversation_summariser needs |
| T-03-08b | Information Disclosure | DeepSeek remote egress | accept | DeepSeek is opt-in (empty key = no egress); operator explicitly enables it by setting the key; documented per D-04 |
| T-03-SC | Tampering | package installs | accept | No package installs this phase (httpx/aiohttp/anthropic already pinned) |
</threat_model>

<verification>
- `pytest tests/agent/test_llm_router.py -x --tb=short` green (including the pre-existing test_claude_model regression)
- `python -c "from backend.agent.llm_router import LLMRouter"` imports clean
</verification>

<success_criteria>
- Task-based routing: orchestration→Claude, compaction→Qwen, deepseek→optional gated provider
- Claude remains a fully supported provider; DeepSeek absence is a safe no-op
</success_criteria>

<output>
Create `.planning/phases/03-orchestration-upgrade/03-02-SUMMARY.md` when done
</output>
