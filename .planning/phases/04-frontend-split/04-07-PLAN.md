---
phase: 04-frontend-split
plan: 07
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04", "04-05", "04-06"]
files_modified:
  - frontend/src/App.jsx
  - frontend/src/App.test.jsx
autonomous: false
requirements: [UI-01, UI-02, UI-03]
user_setup: []

must_haves:
  truths:
    - "App.jsx imports and renders <ChatPane /> as the primary chat interface (not the old inline ChatPanel) — UI-01"
    - "App.jsx imports the extracted panels and useWebSocket/constants/format instead of defining them inline"
    - "Every panel in App.jsx's render tree is individually wrapped in <ErrorBoundary panelName=...> — UI-02, D-05"
    - "App.jsx declares NEW useState for sessionId and chatConnected (neither exists in App.jsx today) and populates them from ChatPane's onSessionChange/onConnectionChange callbacks — App.jsx stays the state owner per D-08"
    - "App.jsx provides SessionContext via a useMemo'd value built from those useState fields (D-08) — UI-03"
    - "App.test.jsx imports the real hooks/useWebSocket.js (inline copy deleted) and asserts App renders ChatPane, not the inline ChatPanel"
    - "The full Vitest suite is green"
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Slimmed composition root: state + layout, importing all extracted modules, ErrorBoundary-wrapped, SessionContext.Provider, sessionId/chatConnected fed by ChatPane callbacks"
      contains: "SessionContext.Provider"
    - path: "frontend/src/App.test.jsx"
      provides: "Updated to import real useWebSocket + render-tree assertion for ChatPane"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "frontend/components/ChatPane.tsx"
      via: "import ChatPane + render with onSessionChange={setSessionId} onConnectionChange={setChatConnected}"
      pattern: "onSessionChange=\\{setSessionId\\}"
    - from: "frontend/src/App.jsx"
      to: "frontend/context/SessionContext.jsx"
      via: "SessionContext.Provider value={useMemo(...)}"
      pattern: "SessionContext.Provider"
    - from: "frontend/src/App.jsx"
      to: "frontend/components/ErrorBoundary.jsx"
      via: "wraps every panel"
      pattern: "ErrorBoundary panelName"
---

<objective>
Rewrite `App.jsx` from a 1505-line monolith into a slim composition root that imports all the modules extracted in 04-02 through 04-06, wraps every panel in an `ErrorBoundary` (D-05), declares the NEW `sessionId`/`chatConnected` state this phase introduces and populates it from `ChatPane`'s callbacks, provides `SessionContext` via a memoized mirror of its `useState` fields (D-08), and renders `<ChatPane />` in place of the old inline `ChatPanel` — satisfying all three phase requirements at their integration point. Then update `App.test.jsx` to import the real `useWebSocket` and assert the render tree. This is the wave-4 wiring plan; all creation work is already done by prior plans (including ChatPane's `onSessionChange`/`onConnectionChange` output contract, defined and tested in 04-04).

IMPORTANT — new state, not existing: `App.jsx` has NO `sessionId` state today (`grep -ni session frontend/src/App.jsx` returns zero matches), and its old `chatConnected` came from a chat `useWebSocket` call that this plan removes (ChatPane owns the chat socket now). This plan DECLARES both as new `useState` and feeds them from ChatPane's callbacks — do not treat them as pre-existing.

Purpose: Complete UI-01 (ChatPane is the rendered chat interface), UI-02 (panels are fault-isolated in the tree), and UI-03 (session state flows via context) at the composition root.
Output: Slimmed `App.jsx`, updated `App.test.jsx`, full green suite. Concludes with a browser verification checkpoint (React DevTools component tree + error-boundary isolation) that jsdom cannot assert.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-frontend-split/04-CONTEXT.md
@.planning/phases/04-frontend-split/04-PATTERNS.md
@.planning/phases/04-frontend-split/04-RESEARCH.md
@.planning/phases/04-frontend-split/04-UI-SPEC.md
@.planning/phases/04-frontend-split/04-VALIDATION.md
@frontend/context/SessionContext.jsx
@frontend/components/ErrorBoundary.jsx
@frontend/components/ChatPane.tsx

<interfaces>
<!-- All imports below already exist (created in 04-02..04-06). App.jsx keeps its state-owner logic; only sub-component DEFINITIONS are replaced with imports. -->
Keep unchanged in App.jsx: the state-owner logic (useState/useCallback/useEffect: health/directives fetch, WS handlers handleEventMessage/handleTerminalMessage, handleSendMessage/handleSetScope/handleGateResolve/handleSendDirective, the events (/ws) and terminal (/ws/terminal) useWebSocket calls).
REMOVE from App.jsx (ChatPane now owns the chat socket): the `useWebSocket(\`${WS_BASE}/chat\`, handleChatMessage)` call (currently line ~1380) and its `handleChatMessage` callback (lines ~1347-1377). ChatPane owns and drives the chat socket internally via the shared hook.
DECLARE new in App.jsx (neither exists today): `const [sessionId, setSessionId] = useState(null)` and `const [chatConnected, setChatConnected] = useState(false)`. These replace the removed chat-socket-derived `chatConnected` and add the never-before-present `sessionId`.
POPULATE them from ChatPane's tested output contract (from 04-04): render `<ChatPane ... onSessionChange={setSessionId} onConnectionChange={setChatConnected} />`. ChatPane calls `onSessionChange(session_id)` on the {type:'session'} ack and `onConnectionChange(true/false)` on ack/disconnect. App.jsx remains the state owner (D-08); ChatPane owns the socket.
Replace: inline sub-component definitions (lines 188-1221) → imports from frontend/components/*, frontend/hooks/useWebSocket, frontend/lib/constants, frontend/lib/format.
Wrap: render tree (lines 1439-1505) in <SessionContext.Provider value={sessionValue}> and each panel individually in <ErrorBoundary panelName="…">.
sessionValue = useMemo(() => ({ sessionId, chatConnected, eventsConnected, terminalConnected, scope, currentPlan, engagementActive, engagementStart }), [<each field>]).  (chatConnected reflects ChatPane's session-acked state, delivered via onConnectionChange.)
ChatPane import path from src/App.jsx: '../components/ChatPane' (ChatPane.tsx lives in frontend/components/, sibling to panels). ChatPane props: { pendingGate, onGateResolve, onSessionChange, onConnectionChange }.
App.test.jsx: delete the inline useWebSocket copy (lines ~12-163, keep MockWebSocket), import { useWebSocket } from '../hooks/useWebSocket'; add a render assertion that ChatPane (not inline ChatPanel) is rendered.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.jsx — swap inline definitions for imports and wrap every panel in ErrorBoundary</name>
  <files>frontend/src/App.jsx</files>
  <read_first>
    - frontend/src/App.jsx (full file — the inline sub-component defs at lines 188-1221 to delete, and the render tree at lines 1439-1505 to wrap)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the `frontend/src/App.jsx (rewrite — composition root)` section — the exact import block and wrapping instructions)
    - frontend/components/ErrorBoundary.jsx (the boundary to wrap each panel with)
    - All extracted component files from 04-04/04-05/04-06 (confirm default-export names/prop signatures to import correctly)
  </read_first>
  <action>
    Rewrite the import block and component-definition region of `frontend/src/App.jsx`. Replace the import block (current lines 1-7) with imports of: React hooks including `useMemo`; only the `lucide-react` icons App.jsx itself still uses directly (likely none once panels are extracted); `useWebSocket` from `../hooks/useWebSocket`; `SessionContext` from `../context/SessionContext`; `ErrorBoundary` from `../components/ErrorBoundary`; each extracted panel (`StatusBar`, `ScopePanel`, `DirectivesPanel`, `TerminalPanel`, `FindingsPanel`, `AgentTracker`, `PlanPanel`, `HealthPanel` from `../components/*`) plus `ChatPane` from `../components/ChatPane`; and any shared constant/formatter from `../lib/*`. Delete the inline sub-component definitions (lines 188-1221) now that they live in their own files. Wrap the entire render tree (lines 1439-1505) in `<SessionContext.Provider value={sessionValue}>` (the `sessionValue` itself is built in Task 2 — for this task, provide it as a placeholder pass-through of existing values so the tree compiles, or leave the Provider wrapping and complete the value in Task 2), and wrap EACH of the 9 panels (all extracted panels including ChatPane) individually in `<ErrorBoundary panelName="…">` (D-05 — every panel, not just the 3 ROADMAP-named ones; use a descriptive panelName per panel). Preserve the existing 3-column grid layout structure (Claude's discretion per CONTEXT.md — keep it unless the split makes a cleaner structure obvious). Pass the same callbacks the panels expect (`handleSetScope`→ScopePanel `onSetScope`, `handleSendDirective`→DirectivesPanel `onSendDirective`, `fetchHealth`→HealthPanel `onRefresh`, etc.). Leave the chat-socket removal and sessionId/chatConnected wiring to Task 2 — this task focuses on imports + panel wrapping to keep the diff reviewable.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/App.jsx','utf8'); if(!/import ChatPane from '\\.\\.\\/components\\/ChatPane'/.test(s))throw new Error('ChatPane not imported'); if(/function ChatPanel/.test(s))throw new Error('old inline ChatPanel still defined'); const eb=(s.match(/ErrorBoundary panelName/g)||[]).length; if(eb<8)throw new Error('panels not individually ErrorBoundary-wrapped, found '+eb); console.log('App.jsx imports+wrapping OK, '+eb+' ErrorBoundary wraps')"</automated>
  </verify>
  <acceptance_criteria>
    - `App.jsx` imports `ChatPane` from `../components/ChatPane` and renders it; the old inline `function ChatPanel` definition is gone
    - `App.jsx` imports the 8 extracted panels + `useWebSocket` + `SessionContext` + `ErrorBoundary` (no inline panel definitions remain)
    - At least 8 `<ErrorBoundary panelName=...>` wrappers exist (every panel individually, D-05)
    - The 3-column grid layout is preserved
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: App.jsx — declare sessionId/chatConnected state, remove the chat socket, thread ChatPane callbacks, build the memoized SessionContext value</name>
  <files>frontend/src/App.jsx</files>
  <read_first>
    - frontend/src/App.jsx (state-owner logic ~1224-1436; the chat `useWebSocket` call ~1380 and `handleChatMessage` ~1347-1377 to remove)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the SessionContext.Provider value + memoization section)
    - .planning/phases/04-frontend-split/04-RESEARCH.md (Architecture Patterns: the System Architecture Diagram showing the SessionContext.Provider tree; Pattern 2 memoization pitfall)
    - frontend/context/SessionContext.jsx (the SessionContext to provide)
    - frontend/components/ChatPane.tsx (its onSessionChange/onConnectionChange output contract — the props to wire)
  </read_first>
  <action>
    Complete the composition root's state + context wiring in `frontend/src/App.jsx`. First, REMOVE App.jsx's own chat socket: delete the `useWebSocket(\`${WS_BASE}/chat\`, handleChatMessage)` call (~line 1380) and the `handleChatMessage` callback (~lines 1347-1377) — ChatPane owns the chat socket now. Keep the events (`/ws`) and terminal (`/ws/terminal`) `useWebSocket` calls untouched. Second, DECLARE the two new state fields near the other `useState` hooks (~line 1224-1240): `const [sessionId, setSessionId] = useState(null)` and `const [chatConnected, setChatConnected] = useState(false)` — NEITHER exists in App.jsx today (App.jsx has never had a `sessionId`, and the previous `chatConnected` came from the now-removed chat socket). Third, THREAD ChatPane's tested callbacks: render ChatPane as `<ChatPane pendingGate={pendingGate} onGateResolve={handleGateResolve} onSessionChange={setSessionId} onConnectionChange={setChatConnected} />` so ChatPane reports its negotiated session_id and session-acked status up into App.jsx state (per the 04-04 output contract) — App.jsx remains the state owner (D-08). Fourth, BUILD `const sessionValue = useMemo(() => ({ sessionId, chatConnected, eventsConnected, terminalConnected, scope, currentPlan, engagementActive, engagementStart }), [sessionId, chatConnected, eventsConnected, terminalConnected, scope, currentPlan, engagementActive, engagementStart])` — list every field individually in the dep array (RESEARCH.md Pattern 2 pitfall: never pass an inline object literal). Fifth, ensure the `<SessionContext.Provider value={sessionValue}>` wrapping (added in Task 1) now consumes this real memoized value. Do not reintroduce prop-drilling for session fields — consumers read them via `useContext(SessionContext)` / the `useSession` hook.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/App.jsx','utf8'); if(/WS_BASE\\}\\/chat[^/]/.test(s)||/[\`'\"]\\/chat[\`'\"]/.test(s))throw new Error('App.jsx still owns a chat socket'); if(!/const \\[sessionId, setSessionId\\] = useState/.test(s))throw new Error('sessionId state not declared'); if(!/const \\[chatConnected, setChatConnected\\] = useState/.test(s))throw new Error('chatConnected state not declared'); if(!/onSessionChange=\\{setSessionId\\}/.test(s))throw new Error('ChatPane onSessionChange not wired'); if(!/onConnectionChange=\\{setChatConnected\\}/.test(s))throw new Error('ChatPane onConnectionChange not wired'); if(!/SessionContext\\.Provider/.test(s))throw new Error('no SessionContext.Provider'); if(!/useMemo/.test(s))throw new Error('no useMemo for context value'); console.log('App.jsx state+context wiring OK')"</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx no longer owns a chat socket (the `${WS_BASE}/chat` `useWebSocket` call and `handleChatMessage` are removed); events + terminal sockets remain
    - App.jsx declares NEW `const [sessionId, setSessionId] = useState(null)` and `const [chatConnected, setChatConnected] = useState(false)`
    - ChatPane is rendered with `onSessionChange={setSessionId}` and `onConnectionChange={setChatConnected}` — sessionId/chatConnected genuinely flow from ChatPane up into App.jsx state
    - `sessionValue = useMemo(...)` lists all 8 D-07 fields individually in its dependency array, and is the value passed to `<SessionContext.Provider>`
    - The verify node script prints the wiring-OK line
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update App.test.jsx — import real useWebSocket, assert ChatPane render tree</name>
  <files>frontend/src/App.test.jsx</files>
  <read_first>
    - frontend/src/App.test.jsx (full file — lines 12-38 MockWebSocket to keep; lines ~40-163 inline useWebSocket copy with the `TODO(Task 3)` marker to delete; lines 167-308 behavior assertions to keep unchanged)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the `frontend/src/App.test.jsx (modify)` section)
    - .planning/phases/04-frontend-split/04-VALIDATION.md (the UI-01 "App.jsx renders <ChatPane/>" test line)
    - frontend/hooks/useWebSocket.js (the real module to import)
  </read_first>
  <action>
    In `frontend/src/App.test.jsx`: delete the inline `useWebSocket` function copy (the block carrying the `TODO(Task 3): Delete this inline copy after App.jsx is updated` comment, lines ~40-163) and replace it with `import { useWebSocket } from '../hooks/useWebSocket'`. Keep the `MockWebSocket` test double (lines 12-38) and `global.WebSocket = MockWebSocket` — the extracted hook's observable behavior is identical, so the existing hook assertions (heartbeat timing, backoff, `{ type: 'reconnect', last_seq: 0 }` payload) must pass unchanged. Add one new test that renders `<App />` (with WebSocket mocked) using `@testing-library/react` `render` and asserts `ChatPane` — not the old inline `ChatPanel` — is what renders the chat UI: query for a stable element unique to `ChatPane` (e.g. the empty-state copy "Ready for operator input" from the UI-SPEC Copywriting Contract, or a ChatPane-specific testid) and assert it is present. Do NOT change the existing hook-behavior assertions.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `App.test.jsx` imports `useWebSocket` from `../hooks/useWebSocket`; the inline copy and its `TODO(Task 3)` marker are gone
    - The `MockWebSocket` double is retained; existing hook-behavior assertions pass unchanged
    - A new render-tree assertion confirms `<App />` renders `ChatPane` (asserting a ChatPane-unique element such as the "Ready for operator input" empty state)
    - `cd frontend && npx vitest run` — the FULL suite is green (App + ChatPane + ErrorBoundary + SessionContext + both panel smoke suites)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser verification — DevTools tree, error-boundary isolation, chat connect</name>
  <action>After the full Vitest suite is green, pause for the operator to run the browser-only checks that jsdom cannot assert: confirm via React DevTools that ChatPane (not ChatPanel) renders the chat UI (UI-01), force a panel throw and confirm ErrorBoundary isolation without white-screening siblings (UI-02), and confirm session-sourced status (UI-03). Await approval before completing the plan.</action>
  <what-built>App.jsx now renders ChatPane as the chat interface, wraps every panel in an ErrorBoundary, declares sessionId/chatConnected state fed by ChatPane's callbacks, and provides SessionContext. The full Vitest suite is green. Two of Phase 4's success criteria can only be confirmed in a real browser (jsdom cannot inspect the React DevTools component tree, nor visually confirm the grid does not white-screen when a panel throws) — per 04-VALIDATION.md "Manual-Only Verifications".</what-built>
  <how-to-verify>
    1. Start the app (`cd frontend && npm run dev`) with the backend running (so `/ws/chat` is reachable). Open the app in a browser.
    2. UI-01: Open React DevTools, expand the component tree, and confirm `ChatPane` (NOT `ChatPanel`) is the component wrapping the visible chat UI.
    3. UI-01 (functional): Confirm the chat connects — the header shows the emerald `dot-live` (session-acked) state, and a typed message round-trips (requires the backend running with the matching bearer token; default `dev-token`).
    4. UI-02: Temporarily force a throw inside one extracted panel (e.g. add `throw new Error('test')` in one panel's render, or use a dev toggle), reload, and confirm that panel's slot shows the "This panel crashed" ErrorBoundary fallback while the OTHER panels remain rendered and interactive (no full-page white-screen). Remove the temporary throw afterward.
    5. UI-03: (optional visual confirmation) Confirm StatusBar's connection dots reflect session state sourced via context, and the chat header's connected dot reflects the sessionId/chatConnected fed up from ChatPane.
  </how-to-verify>
  <read_first>
    - .planning/phases/04-frontend-split/04-VALIDATION.md (Manual-Only Verifications table)
    - .planning/ROADMAP.md (Phase 4 success criteria 1-3)
  </read_first>
  <acceptance_criteria>
    - Operator confirms React DevTools shows `ChatPane` wrapping the chat UI (UI-01)
    - Operator confirms a forced panel throw isolates to that panel's ErrorBoundary fallback without crashing siblings (UI-02)
    - Operator has removed any temporary throw used for the test
    - Operator types "approved" or reports specific issues
  </acceptance_criteria>
  <resume-signal>Type "approved" if all three browser checks pass, or describe what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel crash → dashboard render tree | A single panel's render error must not unmount the whole app |
| Session state → context consumers | Session metadata flows to all components via context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04c | Denial of Service (single-panel crash white-screens app) | App.jsx render tree | mitigate | Every panel is individually wrapped in `<ErrorBoundary panelName=…>` (D-05); a crash in one is contained to its subtree. The checkpoint's step 4 visually confirms sibling survival. Verify asserts ≥8 boundary wraps. |
| T-04-02e | Tampering / XSS | Composition root render | mitigate | App.jsx renders only the already-XSS-safe extracted components (JSX escaping, no `dangerouslySetInnerHTML`); no new render sink introduced at the composition layer. |
| T-04-03c | Session Management | SessionContext value (sessionId / chatConnected) | accept | `sessionId` and `chatConnected` are NEW state this phase introduces in App.jsx (App.jsx had no `sessionId` before, and its `chatConnected` previously came from a chat socket removed here). They are now fed by ChatPane's `onSessionChange`/`onConnectionChange` callbacks — the `session_id` provenance is unchanged (server-issued via the ChatPane-owned `init`→`session` handshake, D-03), App.jsx merely stores and mirrors it into context (D-08). No new client-trust surface: context distributes a server-issued id, it does not mint or validate one. |
| T-04-SC | Tampering | No new package installs | accept | This plan adds no dependencies; imports only already-approved modules and prior-plan artifacts. |
</threat_model>

<verification>
- `cd frontend && npx vitest run` — full suite green (all prior-plan test files + App.test.jsx)
- `grep -c "ErrorBoundary panelName" frontend/src/App.jsx` ≥ 8; `grep -c "function ChatPanel" frontend/src/App.jsx` == 0
- `grep -n "const \[sessionId, setSessionId\] = useState" frontend/src/App.jsx` present; `grep -n "onSessionChange={setSessionId}" frontend/src/App.jsx` present
- `grep -n "SessionContext.Provider" frontend/src/App.jsx` present with a `useMemo`'d value
- Manual browser checkpoint confirms UI-01 DevTools tree, UI-02 fault isolation, UI-03 context-sourced state
</verification>

<success_criteria>
App.jsx is a slim composition root: it renders `ChatPane` (UI-01), wraps every panel in an `ErrorBoundary` for fault isolation (UI-02), and distributes session state — including the newly-declared `sessionId`/`chatConnected` fed up from ChatPane's callbacks — via a memoized `SessionContext.Provider` without prop-drilling (UI-03). The full Vitest suite is green and the browser checkpoint confirms the three ROADMAP success criteria that automated tests cannot reach.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-split/04-07-SUMMARY.md` when done.
</output>
