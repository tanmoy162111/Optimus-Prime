---
phase: 04-frontend-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.js
  - frontend/pages/index.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03]
user_setup: []

must_haves:
  truths:
    - "`cd frontend && npm ci` succeeds without EUSAGE and resolves the real Vite/Vitest/React toolchain"
    - "`cd frontend && npm run test` invokes vitest (script exists)"
    - "No Next.js page files remain under frontend/pages/"
    - "The chat WebSocket path /ws/chat resolves through the existing /ws proxy prefix; no dead /chat proxy entry remains"
  artifacts:
    - path: "frontend/package.json"
      provides: "Dependency manifest aligned with package-lock.json (name optimus-prime-ui, Vite toolchain)"
      contains: "\"vitest\""
    - path: "frontend/package-lock.json"
      provides: "Regenerated lockfile matching the corrected package.json"
    - path: "frontend/vite.config.js"
      provides: "Proxy config with the dead /chat entry removed, /ws prefix retained"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/package-lock.json"
      via: "npm install regeneration"
      pattern: "optimus-prime-ui"
---

<objective>
Fix the frontend build toolchain so `npm ci`/`npm install`/`npm test` work correctly, and remove the dead Next.js remnants. `frontend/package.json` currently declares a Next.js/Socket.IO/Zustand toolchain (`optimus-frontend` 1.0.0) while `frontend/package-lock.json` is the real Vite/Vitest/Testing-Library/lucide-react/Tailwind toolchain the live code uses (`optimus-prime-ui` 2.0.0). `npm ci` fails with EUSAGE today; `npm install` "succeeds" but silently regenerates the lockfile to the wrong dependency set, dropping `vite`/`vitest`/`lucide-react`/`tailwindcss`. This plan is BLOCKING — no other Phase 4 plan can run its tests until this is fixed (D-10 escalation, per RESEARCH.md Pitfall 1).

Purpose: Make the toolchain buildable/testable and delete the dead Next.js page (D-10) so downstream component work has a working test harness.
Output: Corrected package.json, regenerated package-lock.json, deleted pages/index.tsx, cleaned vite.config.js.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-split/04-CONTEXT.md
@.planning/phases/04-frontend-split/04-RESEARCH.md
@.planning/phases/04-frontend-split/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite frontend/package.json to match package-lock.json's real dependency tree</name>
  <files>frontend/package.json</files>
  <read_first>
    - frontend/package.json (current broken Next.js manifest, 21 lines)
    - frontend/package-lock.json (source of truth — read the `packages[""]` block for the real name/version/dependencies/devDependencies)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the `frontend/package.json (config fix)` section, lines ~438-481, gives the exact target manifest verbatim)
    - .planning/phases/04-frontend-split/04-RESEARCH.md (Standard Stack section: "To Remove from package.json" table and the missing-devDependencies table)
  </read_first>
  <action>
    Overwrite frontend/package.json to exactly match the target manifest in 04-PATTERNS.md's "frontend/package.json (config fix)" section. Set `name` to `optimus-prime-ui`, `version` to `2.0.0`, `private` true. Set `scripts` to `dev: "vite"`, `build: "vite build"`, `start: "vite preview"`, `test: "vitest run"`. Set `dependencies` to exactly `lucide-react ^0.383.0`, `react ^18.3.1`, `react-dom ^18.3.1`. Set `devDependencies` to exactly: `@testing-library/jest-dom ^6.9.1`, `@testing-library/react ^16.3.2`, `@testing-library/user-event ^14.6.1`, `@types/react ^18.3.1`, `@types/react-dom ^18.3.1`, `@vitejs/plugin-react ^4.3.1`, `@vitest/coverage-v8 ^4.1.3`, `autoprefixer ^10.4.20`, `jsdom ^29.0.2`, `postcss ^8.4.47`, `tailwindcss ^3.4.14`, `typescript ^5.4.0`, `vite ^5.4.10`, `vitest ^4.1.3`. Do NOT add `next`, `socket.io-client`, `zustand`, or `@types/node` (per RESEARCH.md "To Remove" rationale — `next`/`socket.io-client` are replaced by D-01/D-10, `zustand` is unused and not even in the lockfile). Do NOT bump any version to a newer major than listed — RESEARCH.md verified that vitest 5 / vite 8 produce a real ERESOLVE peer conflict in this sandbox; the pinned versions above are already mutually compatible.
  </action>
  <verify>
    <automated>cd frontend && node -e "const p=require('./package.json'); const dev=p.devDependencies||{}; const dep=p.dependencies||{}; if(p.name!=='optimus-prime-ui')throw new Error('name'); if(!p.scripts.test)throw new Error('no test script'); ['vite','vitest','tailwindcss','@testing-library/react','@vitejs/plugin-react'].forEach(k=>{if(!dev[k])throw new Error('missing dev '+k)}); ['next','socket.io-client','zustand'].forEach(k=>{if(dep[k]||dev[k])throw new Error('dead dep present '+k)}); if(!dep['lucide-react']||!dep['react'])throw new Error('missing runtime dep'); console.log('package.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` `name` field equals `optimus-prime-ui`, `version` equals `2.0.0`
    - `scripts.test` equals `vitest run`; `scripts.dev` equals `vite`
    - `devDependencies` contains `vite`, `vitest`, `tailwindcss`, `@testing-library/react`, `@vitejs/plugin-react`, `jsdom`, `postcss`, `autoprefixer`, `typescript`
    - `dependencies` contains `lucide-react`, `react`, `react-dom` and nothing else
    - Neither `dependencies` nor `devDependencies` contains `next`, `socket.io-client`, or `zustand`
    - The verify node script prints `package.json OK`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task: Package legitimacy gate — confirm vitest before npm install</name>
  <action>Pause before running npm install. Present the vitest [SUS] slopcheck flag to the operator as a package-legitimacy audit-trail checkpoint (documented false positive per RESEARCH.md). Await explicit approval before the next task runs npm install. This gate is not auto-approvable.</action>
  <what-built>package.json has been rewritten to the real Vite/Vitest toolchain. The next task runs `npm install`, which fetches/resolves packages. Per RESEARCH.md's Package Legitimacy Audit, `slopcheck` flagged `vitest` as [SUS] ("suspiciously close to 'vite' — could be a typosquat"). RESEARCH.md documents this as a heuristic FALSE POSITIVE: `vitest` is the official Vite-ecosystem test runner (vitest.dev, `vitest-dev/vitest`), already present with a real integrity hash in this repo's committed `package-lock.json`, and already imported by name in `frontend/src/App.test.jsx` and `frontend/vite.config.js`. This checkpoint exists purely to satisfy the package-legitimacy audit trail before install — not because a real risk was found.</what-built>
  <how-to-verify>
    1. Confirm `vitest` on the npm registry resolves to the official package: visit https://www.npmjs.com/package/vitest and confirm the repository is `vitest-dev/vitest`.
    2. Confirm no OTHER dependency in the rewritten package.json is unfamiliar (all are React/Vite/Testing-Library ecosystem packages listed in RESEARCH.md's Package Legitimacy Audit, all marked OK except the vitest false positive).
    3. Approve to proceed with `npm install`, or report any package you do not recognize.
  </how-to-verify>
  <read_first>
    - .planning/phases/04-frontend-split/04-RESEARCH.md (Package Legitimacy Audit section, including the "Note on the vitest [SUS] flag")
  </read_first>
  <acceptance_criteria>
    - Operator has confirmed `vitest` resolves to `vitest-dev/vitest` on npmjs.com
    - Operator has typed "approved" (or equivalent) to proceed
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with npm install, or name any package to halt on.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Regenerate lockfile, delete pages/index.tsx, remove dead /chat proxy entry</name>
  <files>frontend/package-lock.json, frontend/pages/index.tsx, frontend/vite.config.js</files>
  <read_first>
    - frontend/vite.config.js (34 lines — confirm the `/ws` proxy prefix is at lines 16-20 with `ws: true`, and the dead `/chat` proxy entry is at lines 21-25)
    - frontend/pages/index.tsx (16 lines — confirm it references the nonexistent `LivePanel` and is self-contained/dead)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the `frontend/vite.config.js (minor edit)` section and `frontend/pages/index.tsx (delete)` section)
  </read_first>
  <action>
    First, delete `frontend/pages/index.tsx` (dead Next.js page referencing the nonexistent `LivePanel`, per D-10 — no other file imports from `frontend/pages/`, so the directory becomes empty and can be removed). Second, in `frontend/vite.config.js`, remove the dead `/chat` proxy entry (currently lines ~21-25): the real chat route is `/ws/chat`, already covered by the existing `/ws` proxy prefix rule (lines ~16-20, `ws: true`), so the standalone `/chat` entry proxies to a nonexistent backend path and must be removed. Leave the `/ws` entry and all REST proxy entries (`/health`, `/directives`, `/scope`, `/gate`, `/report`, `/terminal`) unchanged — several target routes that do not exist in the current backend, but that is a pre-existing out-of-scope condition (RESEARCH.md Pitfall 5), not this phase's concern. Third, run `cd frontend && npm install` to regenerate `package-lock.json` so it matches the corrected package.json (do NOT use `npm ci` — the current lockfile must be regenerated, not verified-against). Commit the regenerated lockfile.
  </action>
  <verify>
    <automated>cd frontend && test ! -e pages/index.tsx && ! grep -q "'/chat'" vite.config.js && npm ci >/dev/null 2>&1 && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/pages/index.tsx` no longer exists
    - `frontend/vite.config.js` contains no `'/chat'` proxy key (grep returns nothing) but still contains the `'/ws'` key with `ws: true`
    - `cd frontend && npm ci` completes with exit code 0 (no EUSAGE) — lockfile and package.json are now in sync
    - `frontend/package-lock.json` `name` field equals `optimus-prime-ui`
    - `npx vitest run` executes (passes with existing tests, or `passWithNoTests` — does not error on missing toolchain)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party package code is fetched and resolved into the build tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | npm install (package.json dependency set) | mitigate | slopcheck was run in RESEARCH.md against the exact proposed dependency set; the only flag (`vitest` [SUS]) is a documented false positive; a blocking-human checkpoint precedes `npm install` per package-legitimacy protocol. All packages are already resolved in the committed lockfile with real integrity hashes. |
| T-04-01b | Tampering | package.json/package-lock.json mismatch | mitigate | Aligning package.json to the lockfile removes the risk that a stray `npm install` silently swaps in the wrong (Next.js/Socket.IO) dependency tree and drops the real toolchain |
</threat_model>

<verification>
- `cd frontend && npm ci` exits 0 (was EUSAGE before this plan)
- `cd frontend && npm run test` invokes vitest
- `frontend/pages/` contains no dead Next.js page
- `frontend/vite.config.js` has no dead `/chat` proxy entry
</verification>

<success_criteria>
The frontend toolchain builds and tests run: `npm ci` succeeds, `npm run test` invokes vitest, the Next.js page remnant is gone, and the chat socket path `/ws/chat` resolves via the existing `/ws` proxy prefix. All downstream Phase 4 plans can now run their test suites.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-split/04-01-SUMMARY.md` when done.
</output>
