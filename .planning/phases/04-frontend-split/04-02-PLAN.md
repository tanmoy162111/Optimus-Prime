---
phase: 04-frontend-split
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/hooks/useWebSocket.js
  - frontend/lib/constants.js
  - frontend/lib/format.js
autonomous: true
requirements: [UI-01, UI-02]
user_setup: []

must_haves:
  truths:
    - "A single shared useWebSocket implementation exists as an importable module (D-09) with unchanged reconnect/backoff/heartbeat/StrictMode-guard behavior"
    - "Shared constants (SEVERITY_MAP, EVENT_ICONS, REPORT_FORMATS_UI, REPORT_FRAMEWORKS) and formatters (fmtTime, fmtElapsed, renderPayload) exist as importable modules"
  artifacts:
    - path: "frontend/hooks/useWebSocket.js"
      provides: "Shared WebSocket hook (reconnect/backoff/heartbeat/mountedRef guard)"
      exports: ["useWebSocket"]
      min_lines: 100
    - path: "frontend/lib/constants.js"
      provides: "SEVERITY_MAP, EVENT_ICONS, REPORT_FORMATS_UI, REPORT_FRAMEWORKS"
      exports: ["SEVERITY_MAP", "EVENT_ICONS", "REPORT_FORMATS_UI", "REPORT_FRAMEWORKS"]
    - path: "frontend/lib/format.js"
      provides: "fmtTime, fmtElapsed, renderPayload"
      exports: ["fmtTime", "fmtElapsed", "renderPayload"]
  key_links:
    - from: "frontend/hooks/useWebSocket.js"
      to: "frontend/src/App.test.jsx (existing MockWebSocket-based tests)"
      via: "named export useWebSocket, tested for observable reconnect/heartbeat behavior"
      pattern: "export function useWebSocket"
---

<objective>
Extract the three shared primitive modules that ChatPane and the extracted panels will import: the generic `useWebSocket` hook (D-09 — currently duplicated between `App.jsx` lines ~66-184 and `App.test.jsx` lines ~47-163), and the shared `constants.js`/`format.js` modules (currently module-level consts/utilities in `App.jsx`). These are verbatim relocations — no behavior change. This is a foundation plan: `ChatPane.tsx` (04-04) imports `useWebSocket`, and every data-rendering panel (04-05, 04-06) imports from `constants`/`format`.

Purpose: Establish one canonical home for shared logic so downstream extraction plans import instead of duplicate.
Output: `frontend/hooks/useWebSocket.js`, `frontend/lib/constants.js`, `frontend/lib/format.js` (all new files). Does NOT edit `App.jsx` — the App.jsx rewrite (04-07) swaps its inline copies for imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-frontend-split/04-CONTEXT.md
@.planning/phases/04-frontend-split/04-PATTERNS.md
@.planning/phases/04-frontend-split/04-RESEARCH.md

<interfaces>
<!-- Extracted from frontend/src/App.jsx — the executor moves these verbatim, only the module boundary changes. -->
useWebSocket signature (App.jsx line ~67): useWebSocket(url, onMessage, enabled = true) → returns the same object it returns today (send fn + connected flag). Preserve the mountedRef StrictMode guard, getBackoffDelay exponential backoff, startHeartbeat/stopHeartbeat, visibilitychange reconnect-on-wake, and lastSeq tracking exactly.
constants.js exports (App.jsx lines 15-52): SEVERITY_MAP, EVENT_ICONS (values are lucide-react icon components — import every icon used), REPORT_FORMATS_UI, REPORT_FRAMEWORKS.
format.js exports (App.jsx lines 54-63 + 459-479): fmtTime, fmtElapsed, renderPayload.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract useWebSocket hook to frontend/hooks/useWebSocket.js (verbatim relocation)</name>
  <files>frontend/hooks/useWebSocket.js</files>
  <read_first>
    - frontend/src/App.jsx (lines 66-184 — the canonical useWebSocket implementation to move verbatim)
    - frontend/src/App.test.jsx (lines 12-163 — the inline MockWebSocket + duplicate useWebSocket copy carrying a `TODO(Task 3): Delete this inline copy after App.jsx is updated` marker; this is the behavioral spec the extracted module must satisfy)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the `frontend/hooks/useWebSocket.js (hook, streaming)` section)
    - .planning/phases/04-frontend-split/04-RESEARCH.md (Pitfall 6 — StrictMode double-invoke; the mountedRef guard must not be simplified away)
  </read_first>
  <action>
    Create `frontend/hooks/useWebSocket.js`. Move the function body from `frontend/src/App.jsx` lines 67-183 verbatim into a named export `export function useWebSocket(url, onMessage, enabled = true)`. Add the required React imports at the top: `import { useState, useEffect, useRef, useCallback } from 'react'`. Do NOT change any behavior: preserve the `/health` health-check gate, `getBackoffDelay` exponential backoff, `startHeartbeat`/`stopHeartbeat`, the `mountedRef` StrictMode guard (set true on effect run, checked before creating the socket, set false in cleanup — per RESEARCH.md Pitfall 6, this prevents the StrictMode double-socket bug and must not be removed), the `visibilitychange` reconnect-on-wake listener, and `lastSeq` sequence tracking. This is a pure relocation — the observable behavior (heartbeat timing, backoff timing, the `{ type: 'reconnect', last_seq: 0 }` reconnect payload) must remain identical so the existing `App.test.jsx` assertions still pass unchanged. Do NOT edit `App.jsx` or `App.test.jsx` in this task — those swaps happen in 04-07.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('hooks/useWebSocket.js','utf8'); if(!/export function useWebSocket/.test(s))throw new Error('no named export'); if(!/mountedRef/.test(s))throw new Error('StrictMode guard missing'); if(!/visibilitychange/.test(s))throw new Error('wake-reconnect missing'); if(!/last_seq/.test(s))throw new Error('seq tracking missing'); console.log('useWebSocket OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/hooks/useWebSocket.js` exports `useWebSocket` as a named export with signature `(url, onMessage, enabled = true)`
    - Source contains `mountedRef`, `visibilitychange`, `getBackoffDelay` (or equivalent backoff), heartbeat start/stop, and `last_seq`
    - No import from `socket.io-client` (raw browser `WebSocket` only)
    - `App.jsx` and `App.test.jsx` are NOT modified by this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract shared constants and formatters to frontend/lib/</name>
  <files>frontend/lib/constants.js, frontend/lib/format.js</files>
  <read_first>
    - frontend/src/App.jsx (lines 15-52 for constants; lines 54-63 and 459-479 for formatters)
    - .planning/phases/04-frontend-split/04-PATTERNS.md (the `frontend/lib/constants.js` and `frontend/lib/format.js` sections)
  </read_first>
  <action>
    Create `frontend/lib/constants.js` with plain named exports moved verbatim from `App.jsx`: `SEVERITY_MAP` (lines 15-21), `EVENT_ICONS` (lines 23-46), `REPORT_FORMATS_UI` (lines 48-51), `REPORT_FRAMEWORKS` (line 52). Because `EVENT_ICONS` values reference `lucide-react` icon components (e.g. `Zap`, `Layers`), add an import block at the top of `constants.js` importing every icon referenced by any `EVENT_ICONS` entry from `lucide-react` (cross-check against App.jsx's import list lines 2-7 — import only the icons actually used in these constants, not the whole list). Create `frontend/lib/format.js` with named exports `fmtTime` (App.jsx lines 54-57), `fmtElapsed` (lines 59-63), and `renderPayload` (lines 459-479), moved verbatim. If `renderPayload` returns JSX, keep the file as `.jsx`-compatible — but note Vite handles JSX in `.js` files via esbuild only if configured; if `renderPayload` contains JSX, name the file `frontend/lib/format.jsx` instead and update this task's file path accordingly, otherwise keep `.js`. No logic changes to any function.
  </action>
  <verify>
    <automated>cd frontend && node -e "const c=require('fs').readFileSync('lib/constants.js','utf8'); ['SEVERITY_MAP','EVENT_ICONS','REPORT_FORMATS_UI','REPORT_FRAMEWORKS'].forEach(k=>{if(!new RegExp('export const '+k).test(c))throw new Error('missing '+k)}); if(!/from 'lucide-react'/.test(c))throw new Error('no lucide import for EVENT_ICONS'); const fp=require('fs').existsSync('lib/format.js')?'lib/format.js':'lib/format.jsx'; const f=require('fs').readFileSync(fp,'utf8'); ['fmtTime','fmtElapsed','renderPayload'].forEach(k=>{if(!new RegExp('export const '+k+'|export function '+k).test(f))throw new Error('missing '+k)}); console.log('lib OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/lib/constants.js` has named exports `SEVERITY_MAP`, `EVENT_ICONS`, `REPORT_FORMATS_UI`, `REPORT_FRAMEWORKS`
    - `constants.js` imports every referenced icon from `lucide-react`
    - `frontend/lib/format.(js|jsx)` has named exports `fmtTime`, `fmtElapsed`, `renderPayload`
    - `App.jsx` is NOT modified by this task
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS server → client (via useWebSocket) | Inbound socket payloads are passed to `onMessage` callbacks; the hook itself does not render untrusted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02a | Tampering | useWebSocket onMessage dispatch | accept | The hook only parses and dispatches payloads to caller-supplied handlers; it renders nothing itself. Rendering-side XSS is mitigated in the consuming components (ChatMessage/ChatPane) via React JSX escaping — see 04-04 threat model. No new attack surface introduced by relocation. |
| T-04-SC | Tampering | No new package installs | accept | This plan adds no dependencies; all imports (`react`, `lucide-react`) are already-approved packages from 04-01's audited manifest. |
</threat_model>

<verification>
- `frontend/hooks/useWebSocket.js` exports `useWebSocket` and preserves the StrictMode/heartbeat/backoff behavior
- `frontend/lib/constants.js` and `frontend/lib/format.(js|jsx)` export the named symbols downstream panels need
- `cd frontend && npx vitest run` still passes (existing App.test.jsx behavior tests are unaffected since App.jsx is untouched this plan)
</verification>

<success_criteria>
Three importable shared modules exist with verbatim-relocated logic and zero behavior change. Downstream plans (04-04 ChatPane, 04-05/04-06 panels) can import `useWebSocket`, `constants`, and `format` instead of duplicating them.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-split/04-02-SUMMARY.md` when done.
</output>
